# 2026-09-12 UI 一致性与小屏适配修复记录

## 范围与当前状态

- 基线：本机 `e62f24e`，工作区 `E:\Project\CC-Project\benchshop`。
- 已实现：本记录列出的显示修复和设计规范补充。最新“更新 md，git”授权下，本记录随相关源码及测试纳入本轮本地提交；各增量中的“未提交”保留为当时历史状态。未推送，NAS/ECS 未发布。
- 未修改 API、数据库、PDF 模板、权限、金额或分类规则；未操作真实客户记录。既有工作区原始图片、Excel、PSD 未动。
- 验收使用本地源文件、Edge Headless 和内存 API fixture，所有网络请求在测试上下文截获。**这是布局与前端交互回归，不是三角色真实后端验收。**
- 依据 [项目设计规范](website-design-system.md) 与 [Web Interface Guidelines](https://raw.githubusercontent.com/vercel-labs/web-interface-guidelines/main/command.md)；不为统一样式重做已有正常页面。

## 已修复问题

### 最新增量：小屏已选配置固定底部操作

- 原因：<1024px 时 `.summary-panel` 整层 `overflow-y: auto`，型号、清单及按钮一起滚动。
- 修复：新增 `.summary-content` 包含型号/清单/状态，正文独立滚动；底部仅保留保存、询价按钮，标题和关闭/重置固定。限制动态视口高度，底部使用安全区，低高度使用视口减 16px；桌面包装 `display: contents`，原列表滚动与布局保留。保存失败提示主动滚入可见区域，不覆盖按钮。
- `browser_summary_footer.cjs`：24 组双语、短/40 项长清单、430/320/768/844×390/390×350/1440px 通过；校验滚动前后按钮坐标不变、外层无滚动、正文非零高度、无横向溢出、真实点击区域以及 Escape/焦点恢复。API 全部模拟，不保存真实配置或提交询价。
- 已查看 `tmp/summary-footer/en-430.png` 和 `en-844.png`，58 项契约通过；真机安全区/软键盘仍待验收，本机修改未提交或部署。

### 最新增量：个人中心窄屏导航重排

- 后续用户指定顶栏调整：<900px 使用圆形“中 / EN”与房屋图标两个 44px 控件，语言按钮支持点击当前语言直接切换，桌面维持双语言按钮和主页文字。`browser_account_navigation.cjs` 补验两个圆形点击区、可访问名称、桌面/小屏可见项、430/768px Enter 切换并保留 Hash；132 组专项通过。已查看最新 320px 截图，58 项契约通过；本次未重跑全站 540 项（下方为前一次增量证据）。

- `account/account.css:140`：原横向长标签导航在 320px 下仅能完整显示前两个入口；现为账号摘要、退出登录及两组三列导航，六个入口直接可见。桌面保留左侧分组导航；≤900px 非吸顶顶部布局，低高度自然滚动。
- `account/index.html:39`：原导航增加账号/业务分组容器，原按钮、字段、面板 ID 和路由保留；未读报价徽章不隐藏。`account/account.js` 当前入口的 `aria-current` 修正为 `page`，增加专用键盘焦点样式。
- `account/account.css:167`：表单输入最小宽度修正、窄屏标题与操作区换行、业务筛选采用搜索通栏及双列筛选项；补验发现英文 320px “更多”换行后菜单越过左边界，现限制在记录操作区宽度内。
- 本机专项：`tests/browser_account_navigation.cjs` **132 组面板/尺寸检查通过**，中英文、1440/1024/900/768/640/430/390/360/320px、844×390 及 390×350，含六入口、深链、未读徽章、键盘 Enter/Space、菜单 Escape、草稿保留、原保存参数、长内容、空/失败状态。接口全部内存模拟，无真实客户写入。
- 截图：`tmp/account-navigation/before-en-320-contact.png` 为旧导航证据；`zh-320-profile.png`、`en-320-profile.png`、`en-430-my-quotes.png`、`en-1440-profile.png` 为新布局，已实际查看。浏览器渲染验收不等于 iOS/Android 真机与软键盘验收。
- 前端契约 **58 项通过**，JS 语法通过；本增量后的 `browser_ui_responsive.cjs` 全站 **540 项布局断言通过**（中英文、10 个尺寸，含前台/个人中心/工作台弹窗）。本机已实现并验收；Git 未提交，NAS 未部署。

| 问题 | 复现尺寸 | 修复方式 | 状态 |
| --- | --- | --- | --- |
| 小屏设备目录键盘焦点进入被遮住的一级目录 | 390px，补验 320px 和桌面断点切换 | 一级目录 inert，聚焦与 Tab 范围随可见层同步；空目录聚焦关闭，返回恢复入口 | 已修复，双语专项通过 |
| 排序模式重绘丢失键盘焦点 | 1440px、320px | 开始/取消/插入/保存/失败分别恢复对应控件，异步响应不抢用户新焦点 | 已修复，隔离排序回归通过 |
| 分享备注、分享码及联系销售底部被裁切 | 844×390、390×350 | 限定高度，正文独立滚动，标题/操作不收缩；配额及复用提示纳入正文 | 已实现，布局回归通过 |
| 登录/注册提交按钮越出卡片 | 320×568、844×390 | 表单字段成为可滚动区域，提交与切换入口保留在外部；错误文字参与滚动 | 已实现，布局回归通过 |
| 报价列表零高度或行横向溢出 | 844×390、320×568 | 窄/低屏使用一个正文滚动区，客户摘要、添加栏、列表、总额同区；≤380px 名称单行区、数量/单价/原操作下一行 | 已实现，列表非零高、数量修改/总额、客户弹窗焦点回归通过 |
| 后台侧栏底部超出屏幕 | 1280×600、844×390、320×568 | 100dvh，导航中段滚动，品牌及账户操作不收缩 | 已实现，布局回归通过 |
| 个人中心隐藏主页/退出登录 | ≤640px / ≤900px | 恢复显式入口，窄屏 Logo 适量收窄，保留原事件 | 已实现，入口可见性回归通过 |
| 英文个人中心长内容撑开页面（补验发现） | 390px、320px | 主栅格允许收缩、侧栏最小宽度为零；长保存按钮换行 | 已实现，六个面板双语横向边界回归通过 |
| 长提示购物车确认窗操作被裁切（补验发现） | 320×568 | 仅确认窗正文滚动，保留标题、取消及确认按钮，不影响询价专用弹窗 | 已实现，长文案和 Escape 取消回归通过 |

HTML 新增包装不改变 form、name、按钮 value 与 input 的 data 属性。报价字段标签仅在极窄屏补显，桌面原列表列布局保留。所有新增选择器限定于对应组件；未清除历史 CSS 或旧入口。

## 规范同步

- 设计规范日期更新为 2026-09-12，纳入个人中心；统一“我的分享”和橙色产品编号规则。
- 增加宽度和 `max-height: 520px` 双维适配要求，以及紧凑确认、复杂编辑、详情抽屉三类弹层的滚动约束。
- 修复范围内的触屏主要按钮/关闭按钮提供至少 44px 点击区域；保留工业风、原字号、两处蓝色报价按钮、数量徽章、手机双列选配和购物车两行操作栏。
- 未把所有后台紧凑按钮强制放大；遗留非常规字重、所有旧弹层选择器的规范偏差仍需按组件评估，不能等同于已确认显示缺陷。

## 回归证据

脚本：[tests/browser_ui_responsive.cjs](../tests/browser_ui_responsive.cjs)。需要 Node、Playwright 及本机 Edge；项目根目录执行：

```powershell
# Playwright 不在默认 Node 模块路径时，先将 NODE_PATH 指向已安装模块目录。
node tests/browser_ui_responsive.cjs
$env:UI_TOUCH='1'
node tests/browser_ui_responsive.cjs
Remove-Item Env:UI_TOUCH
```

- 普通指针：540 项布局断言通过，此外执行取消结果、备注值、数量/总额、同型号设备隔离、焦点恢复、页面宽度和无 JS 异常断言。
- 触屏模拟：540 项布局断言及附加交互断言通过；不是 iOS/Android 真机。结果位于 `tmp/ui-responsive-20260912/touch/results-touch.json`，普通指针结果位于 `tmp/ui-responsive-20260912/results.json`。
- 宽高：1440×900、1280×600、1024×768、768×600、640×720、390×844、360×740、320×568、844×390、390×350。
- 中英文通过真实页面语言存储初始化；后台原操作文案继续中文，仅内容/目录/报价语言使用既有设置，不新增后台全站翻译。
- 覆盖：登录/注册及长错误、分享码、联系销售、分享导入长清单、分享备注/配额、购物车/分类预览/工具编辑/询价及错误、前后台确认窗、侧栏、PDF 语言窗、设备和账户弹窗、v2 条目/分类、长报价和客户选择、个人中心六面板和长分享详情。
- 前端数值回归：设备 100 + 18 条工具各 2×10；末条数量改为 3，总额仍为 470。未调用真实保存/发送接口。

### 修复前后截图

截图与机器可读边界数据保存在本机 `tmp/`（不作为发布资源，不加入 Git）：

| 场景 | 修复前 | 修复后 |
| --- | --- | --- |
| 横屏报价零高 | [前](../tmp/ui-audit-20260912/quote-844x390.png) | [后](../tmp/ui-responsive-20260912/quote-zh-844x390.png) |
| 横屏分享备注底部裁切 | [前](../tmp/ui-audit-20260912/share-note-844x390.png) | [后](../tmp/ui-responsive-20260912/share-en-844x390.png) |
| 极窄屏报价行 | 原问题记录于本表 | [320px](../tmp/ui-responsive-20260912/quote-zh-320x568.png) |
| 英文个人中心 | 回归边界报告定位栅格被撑到 561px | [320px](../tmp/ui-responsive-20260912/account-en-320x568.png) |

人工抽查了横屏和 320px 报价、横屏英文分享备注、320px 英文个人中心及触屏模拟横屏报价截图：正文滚动、固定操作及字段标签符合预期。机器通过不代表每张截图已经逐一人工检查。

## CR918S 与设备编辑底栏增量验收

- 取消前台“空分类不加载”的限制，保留当前语言导航过滤；无可见设备时以空状态初始化，不中断购物车和账户功能。目录数据仍保留隐藏设备供历史记录使用。
- 以可用颜色、可用基本配置判断当前设备的前端提交条件；不新增服务端业务规则。标题附近和摘要操作区提示待完善，公共当前配置入口也拦截；异步保存结束按当前设备重算按钮状态。
- 底栏改为专用发布设置组，复选框实际 18×18px；桌面左右分区，窄屏设置与操作分行。语言全不选不发送请求，取消不写数据；有效保存保留 visible_zh/visible_en/enabled/version 参数。
- `tests/browser_catalog_readiness.cjs` 隔离 API 回归通过，涵盖状态恢复、阻止提交/恢复提交、语言回退、空目录以及底栏 1355×838、320×568、844×390。`tests/browser_device_visibility.cjs` 通过；改动后的全站普通/触屏模拟各 540 项布局断言通过。
- 真实页面仅允许 GET 的浏览器核验：CR918S 中文可见、英文隐藏。当前真实配置已有电机和电源，按钮恢复可用；本轮未补写真实设备配置，也未真实提交询价/购物车。
- 截图：[桌面底栏](../tmp/catalog-readiness/footer-1355x838.png)、[窄屏底栏](../tmp/catalog-readiness/footer-320x568.png)、[真实中文设备页](../tmp/catalog-readiness/cr918s-live-zh.png)。已抽查底栏截图；这些文件位于本机临时目录，不属于发布资源。

## 未完成验收（不标记通过）

- [ ] 真实 iOS Safari、Android Chrome 软键盘开合、旋转和安全区；模拟触屏/降低视口高度只能预检。
- [ ] 浏览器真实 200% 缩放；当前仅覆盖等效窄/低视口，不能替代该项。
- [ ] 所有真实角色与加载/失败/禁用业务状态，尤其保存、发送、权限拒绝及下载失败重试；本轮不以 fixture 替代真实后端证明。
- [ ] 全部目录标签、下拉选单、更多菜单、报价历史、审计长表格在各操作系统/浏览器的人工复核；未更改表格结构或删除旧入口。
- [ ] 全站键盘顺序、屏幕阅读器、触控长按/原生 select 与菜单越界专项；本轮验证了部分 Tab、Escape 与嵌套焦点场景，未声明全站无障碍认证。
- [ ] Git 提交、NAS/ECS 发布，单独安排。

## 收尾

本轮代码已实现；最终普通指针与触屏模拟分别 540 项布局断言通过（共 1080 项），附加交互断言通过，无页面 JavaScript 异常。`node --check` 检查 `admin/admin.js`、`js/cart.js` 及新增测试脚本通过；`git diff --check` 通过。测试浏览器已退出，没有启动/停止生产或 NAS 服务，没有提交或暂存文件。上述未完成项继续保留，不因局部布局通过而自动关闭。
