# 2026-09-11 工作计划与执行记录

> 此文件为 2026-09-11 历史实施与验收证据。当前唯一状态表、命令和阻断项见 [2026-09-12 本机维护记录](2026-09-12-maintenance-record.md)；本文件内相互矛盾的阶段性“当前/未实施/未提交”描述不覆盖该记录。

> 2026-09-12 起，本文及其他历史文件中的“本机数据库直接覆盖 NAS”描述已废止；NAS 后续迁移必须执行兼容性合并，现行规则见 [项目执行规则](project-execution-rules.md#3-nas-数据迁移与兼容性合并)。

## 最新检查点：本地文档与 Git 整理

用户已授权更新 Markdown 并创建本地提交；此前“不提交”的文字保留为历史记录。本次不推送、不部署 NAS、不覆盖远端数据库。

- [x] 设备询价请求移除多余 name 字段，修复 422；结构化错误显示可读提示。浏览器模拟接口验证失败提示和成功重试，不写入真实询价。
- [x] 选配分类按钮显示实时数量，气泡复用购物车样式；0 为深灰、大于 0 为橙色，选择/取消验证通过。
- [x] 工具及附件分类列表对齐筛选区；桌面左右 24px、分类间隔 16px，手机左右 16px。独立布局验证 1428px/390px 无页面横向溢出。
- [x] 更新项目说明与 UI 规范，整理累计代码、迁移及测试为本地提交范围。
- [ ] 全项目完整回归及真实业务验收：不能由模拟接口、独立布局或其他专项结果替代。
- [ ] 原始 tb 表格/图片及发布图片跟踪核验：本次不纳入提交，不将发布预检阻塞项标记完成。
- [ ] NAS 部署及远端页面/PDF 验收：本次未授权执行。

## 购物车PDF分享快照复用

- [x] 创建分享新增可选reuse_existing参数，购物车PDF启用；服务端事务内先查本用户有效新旧分享快照再查配额，相同则复用，不延长有效期、不改原备注及快照。
- [x] 双语内容、数量、顺序作为一致性依据，排除购物车行ID和导出语言；界面说明保留原备注，成功后提示复用分享码。满额复用及PDF失败重试浏览器测试通过。
- [x] 服务端并发、跨用户、过期/关闭、满额复用、不同数量及历史分享匹配综合专项通过；原有分享重试专项通过。localhost与局域网HTTP浏览器均验证满额复用及失败重试，前端58项通过。本轮不提交、不部署。

## PDF模块断页修复

- [x] 取消末组带总额表格整块移页及模块固定高度换页阈值；设备、工具、附件间统一12pt间距，表头重复和末条数据/总额不拆分保护保留。
- [x] 11项分页/排版测试通过，中英文边界样稿4页视觉检查通过；附件从前页剩余空间开始并自然续页，总额不变。本机API已重启，未提交或部署。

## PDF语言选择弹窗调整

- [x] 移除设备编辑窗固定高度继承，使用400px以内居中自适应布局和独立内边距；中文版/English并排单选，保留原有导出/取消逻辑与默认语言。
- [x] 1440px与390px浏览器验收：高度低于350px、无横向溢出，方向键选择、导出返回语言、Escape取消和焦点返回通过。仅本机更新，未提交或部署。

## 局域网访问修复

- [x] 购物车PDF请求键兼容HTTP局域网：优先randomUUID，不可用时用getRandomValues生成UUID v4；仍在首次确认时生成并存入同一次导出状态，失败重试不新建分享。更新cart脚本缓存版本。

- [x] 删除 runtime-config 按8081端口推导外部8001接口地址的旧逻辑。本机代理和NAS Nginx均默认同源 `/api/`，保留显式API地址覆盖。
- [x] 更新主页、工作台、个人中心脚本缓存版本；本机通过192.168.31.53:8081浏览器检查，请求全部走8081，产品/ready/后台/个人中心HTTP200；本机、NAS地址及HTTPS默认配置测试通过。
- 原因：前端直连局域网IP:8001，但后端仅监听回环地址。无需开放8001或修改防火墙。NAS标准8080部署原先走同源代理，不受这条8081分支影响；本轮未部署或实际验收NAS。

## 最新执行：PDF 品牌模板统一（本机，未部署）

- [x] 三类 PDF 共用页眉：左 Logo，右公司名称/对应语言地址/网址；支持地址换行与动态正文留白。Logo 缺失或损坏停止生成并返回明确错误。
- [x] 所有页脚统一：左文件编号、中间页码、右北京时间导出时间；编号及金额规则不变。
- [x] 主标题统一加粗；英文报价标题改为 Price List。询价/报价原业务时间移至首页标题下方，文档信息模块保持隐藏。
- [x] 分享清单专属“分享人 / Shared by”，其他客户信息模块不变。
- [x] 本机 API 8001、网站8081启动，主页/后台HTTP200、ready接口通过；没有提交Git、部署NAS或修改数据库结构。
- [x] 新增排版专项9项通过，六种中英文多页样稿共18页图像检查通过；购物车分享后PDF失败重试浏览器测试通过。
- [x] PDF/语言/编号兼容测试23项通过（50项无关用例未选中），新增长地址测试补跑后排版专项9项通过；没有将此次专项验收宣称为全项目全量回归。以下旧实施轮状态保留历史背景。

## 最新实施轮：剩余功能已接入，最终回归中

本节优先于下方历史“未实施”检查点。本轮仅本机实施，没有 Git 提交或 NAS 部署。

- [x] 设备语言显示范围：新增/编辑支持中文、英文及双语；历史默认双语，至少一种；目录过滤不移除配置数据，保留直接访问及购物车历史兼容。
- [x] 设备排序：上移/下移、保存/取消，共用排序；服务端集合及版本校验、事务保存，新增设备排末尾并记录排序审计。
- [x] 业务进度：服务端派生新询价/已分配/已联系/待报价/已报价/已归档/完成/取消；分页前筛选，原状态和统计保留兼容，发送数不包含草稿。
- [x] 购物车 PDF：确认30字符备注后创建分享，再导出该分享快照；普通用户配额继续生效，幂等键绑定用户和请求内容，事务内复核并发请求，PDF失败复用分享重试。
- [x] PDF 命名及页内编号：ShareBench-BOTEN分享码、RFQ-BOTEN原日期-至少两位流水、QUOTA-BOTEN原日期-至少四位流水；各下载入口读取服务端文件名，补充跨域响应头暴露。
- [x] 本机旧编号只读扫描：7份询价/报价记录，未发现异常；不改写原单号。
- [x] 本机迁移至0030。备份：`backend/migration-backups/boten-20260911-114306-787253.db`（变更前）、`boten-20260911-114335-901209.db`。0029最初重建表被外键阻止，已改为增量加列；清除本次产生的0行临时表，7台设备保留，完整性及外键通过。
- [x] 个人中心分享布局/标题，中英文、1440/390及切换其他业务弹窗隔离浏览器验证通过。
- [x] 设备排序取消/保存隔离浏览器测试、购物车同型号分类隔离、分享后PDF失败重试（1次创建、2次导出）通过。
- [x] 工具/附件分类新增、编辑、删除隔离浏览器流程通过；修复关闭目录编辑窗时误移除全局提示节点的问题。设备中英文导航过滤与完整数据保留检查通过。
- [x] 六种中英文PDF样稿生成、文本编号及逐页图像检查通过，Logo和页眉页脚无可见截断；样稿位于私有临时目录 `tmp/pdf-identity`，不是实际客户发送记录。
- [ ] 最终全量回归完成并记录结果；不得用首轮332通过/8失败报告作为最终通过证据。

原有远端部署、真实三角色全流程、Linux/Excel/WPS及安全专项等历史项目不因此轮实现自动完成。API已重启，`/api/v1/ready` 返回ready。

## 最新执行检查点：目录管理与个人中心交互增量

### 当前进度汇总（2026-09-11 核对）

本次仅更新进度文档，未新增代码修改、测试运行或部署；以下验收证据沿用上一执行轮。

| 模块 | 实现进度 | 验收进度 / 下一步 |
| --- | --- | --- |
| 个人中心文案、分享详情 | 已实现 | 语法/源码检查通过；补桌面、手机、中英文及业务弹窗切换验证 |
| 购物车分类只读预览 | 已实现 | 隔离浏览器专项通过；完整业务回归仍待完成 |
| 工具/附件分类扩展 | 已实现 | 隔离数据库及分类筛选浏览器回归通过；补新增/编辑/删除端到端交互 |
| 设备语言可见性及排序 | 未实施 | 下一阶段优先处理，迁移前备份本机数据库 |
| 询价业务进度合并 | 未实施 | 服务端派生统计、分页前筛选及前端展示待实现 |
| 购物车先分享再导出 | 未实施 | 分享幂等、配额、快照导出及失败重试待实现 |
| PDF 文件名及内部编号 | 未实施 | 历史编号只读扫描、统一生成与实际渲染验收待完成 |
| Git / NAS 发布 | 未执行 | 保持暂不提交、不部署的授权边界 |

本轮尚未进行完整全量回归，不将下方历史 331 项测试结果视为本轮验收，也不按功能条目数估算完成百分比。

本节为新增合并需求的状态入口，不覆盖此前已发布基线。仅修改本机 `E:\Project\CC-Project\benchshop`，未提交 Git、未部署 NAS、未迁移真实数据库。

### 已实现内容与验收证据（按项区分）

- 个人中心：我的分享/我的询价按钮为“查看 / View”，询价复制为“复制询价码 / Copy Code”；报价按钮文案保持原样。
- 分享详情：标题追加真实分享码，基本信息改为一项一行、左标题右内容；通过分享专属类隔离其他业务弹窗。已完成源码及语法检查，个人中心真实浏览器验收待补。
- 购物车分类预览：按条目 ID + 分类 ID 只读显示已选项；保留完整详情，关闭恢复焦点。`tests/browser_cart_category_preview.cjs` 桌面/手机、中英文、同型号设备隔离、Escape 焦点及购物车保留通过。
- 工具/附件分类：扩展现有分类创建/编辑后端及前端入口；复用配置分类列表框，保留筛选搜索，无搜索时展示空分类，分类类型由父目录确定。
- 前端契约测试 58 项通过；分类隔离测试通过，包含新增分类创建、重复/嵌套拒绝、编辑、停用及非空删除保护（1 项复合测试，53.93 秒）；`tests/browser_catalog_categories.cjs` 公共/后台分类筛选、搜索、空状态、必填及 1440/390 布局通过。
- JavaScript 语法及 `git diff --check` 通过。测试使用隔离数据库/接口，未写真实业务记录。

### 必须继续实施，尚未完成

- [ ] 设备中文/英文可见性及迁移：历史默认双语，仅过滤目录、不影响直接访问或历史业务。
- [ ] 设备共用顺序编辑：上移/下移、保存/取消、原子写入、并发校验，新设备末尾。
- [ ] 询价业务进度合并：保留原状态，新增草稿/已发送/归档计数，已发送才算已报价，服务端分页前派生筛选；终态优先并保留负责人。
- [ ] 购物车先分享再 PDF：30 字符可选备注、确认才创建、10 份配额、事务幂等、超时及 PDF 失败复用同一分享，从分享快照生成。
- [ ] PDF 文件名及页内文档编号统一：`ShareBench-BOTEN{分享码}`、`RFQ-BOTEN{原日期}-{至少两位流水}`、`QUOTA-BOTEN{原日期}-{至少四位流水}`；文件名加 `.pdf`。工作台和个人中心一致，中英文同名，超位完整保留，不改数据库单号。
- [ ] 实施前只读扫描历史业务编号异常；不随机生成或静默重写旧编号。
- [ ] 补齐个人中心、分类新增/编辑/删除全流程浏览器验证，以及权限、并发、业务状态和真实 PDF 逐页渲染验收。
- [ ] 完成后同步项目设计/聚合规范中旧文件编号说明。本轮尚未改 PDF 生成器，旧编号说明仍代表当前行为。

上述未完成项不能标记为“已实现”或“本机已验收”。远端保持原版本。

## 今日新增需求合并：状态与范围

最新授权：用户要求先完成本机验收，暂不提交 Git 或部署；历史发布/提交授权不自动延续。

## 前阶段查漏修复记录（历史证据）

- [x] 服务端禁止配置/工具/附件跨目录改类，无论是否存在设备引用；同目录改类仍允许。新增拒绝后 category_id/version 不变的回归测试，并同步中英文错误提示。
- [x] 补验三个 PDF 入口失败后释放导出锁并可重试；桌面/手机关闭按钮、Escape、遮罩关闭、焦点和滚动恢复通过。个人中心/报价客户弹窗、分类筛选浏览器测试通过。全部采用隔离接口数据，不代表真人账号完整发送验收。
- [x] 本机与 NAS 当前运行版本只读冒烟均通过：首页、ready、产品、PNG Logo 及数据库/源资料/.env 拒绝访问。NAS 尚未载入本次修复。
- [x] 新增可复用发布预检/打包工具，支持 SQLite 在线备份、代码与私有数据分离、图片/LFS/哈希校验、拒绝重复输出及缺失资源阻断；不执行传输或远端覆盖。
- [x] 本轮全量回归：331 passed、14 subtests passed（299.24 秒，2 条现有测试依赖弃用警告）；随后发布工具完整专项 9 项通过，包含新增 LFS 指针和上传图片缺失阻断。三组浏览器脚本通过，JS 语法与 diff 检查通过。API 已重启载入分类隔离修复，ready 正常。
- [x] 当前本机 Python 环境 `pip check` 通过，`pip-audit` 查询未发现已知漏洞；不代表 Linux 镜像或系统包漏洞扫描已完成。
- 待执行、等待授权及待评估项目统一列于下方当前任务表；不把等待授权当作本机功能缺陷。

下方旧检查点清单和执行日志按历史证据保留；同一条目的旧“未完成/未部署”状态不覆盖本节和新增需求节。完整项目仍未闭环。

## 前阶段遗留待办总表（与顶部新增待办共同执行）

本次仅更新计划，未新增实施或验收结果。状态分为“待执行”“待环境”“待评估”“等待授权”；完成时记录测试入口、运行环境、数据范围和结果，不能仅凭代码存在勾选。

### P1：本机端到端验收（待执行）

- [ ] E2E-01：使用隔离数据库、独立端口和管理员/业务员/客户测试账号，让浏览器连接真实后端，完成分享→导入→询价→分配→报价编辑/发送→客户收件。不能用模拟接口响应替代，不要求操作真实客户记录，不重置真实账号密码。
- [ ] E2E-02：覆盖分享 9→10→满额拒绝、取消跳转保留购物车、删除后旧码失效并释放名额、重新创建，以及关闭/恢复/重新开放规则；并发限制保留 API 专项，浏览器核验提示与状态刷新。
- [ ] E2E-03：在同一隔离环境核验新标签页登录态、角色权限、原页搜索/数量/购物车保留；客户四字段自动填充（含非空电话和地址）、手动修改保存、取消不覆盖、已发送快照不被后续编辑改写。
- [ ] UI-01：补齐关闭按钮默认/悬停/按下/键盘焦点的视觉状态；真实触屏、软键盘和系统缩放单独记录人工复验，不能用桌面浏览器窄窗口代替。

### P1：PDF 验收拆分（部分已验证，剩余待执行）

- [x] PDF-01：三个入口语言选择、取消、防重复及失败重试已通过隔离界面测试；六份双语样稿共 12 页已完成排版检查。
- [ ] PDF-02：在 E2E-01 的真实后端环境，由浏览器下载分享/询价/报价各中英文两份，核验实际文件可打开、图片 Logo、标题、配置分类/编号、页眉页脚、跨页和排序；报价金额不因语言变化，分享/询价不得泄露价格。
- [ ] PDF-03：验证缺少英文译文的历史快照回退原文，不能空白、自动猜译或读取当前目录覆盖；补查销售自定义名称、长文本、同型号多设备和明确清空的客户字段。英文完整翻译覆盖与安全回退是不同验收项。

### P1：发布包异常与恢复演练（待执行，仅隔离目录）

- [x] REL-01：现有专项已覆盖脏工作区、缺失/未跟踪静态图、缺失上传图、LFS 指针、路径越界、输出重名和正常快照归档校验。
- [ ] REL-02：补齐数据库迁移版本与代码不一致、打包过程中源文件变化的拒绝测试；确认没有可误用为成功包的结果，原库与源文件不受损。
- [ ] REL-03：在本机临时暂存目录模拟传输中断或文件损坏，验证接收端清单校验失败即停止；此演练不访问 NAS/ECS。
- [ ] REL-04：在隔离目录恢复数据库及对应上传图片，核验完整性、外键、迁移版本、关键业务内容与图片哈希；验证失败时恢复至演练前状态，记录恢复步骤与证据，不能以“生成备份成功”代替恢复验收。

### P2：既有专项与重构评估

- [ ] SEC-01（待执行）：先逐接口列出可复现的校验、权限、版本冲突或图片引用风险，再决定修复范围。报价全行显式模型收敛、旧目录服务整合列为“待评估重构”，不预设它们本身就是漏洞，不无依据列为发布阻断，不借此改变界面或业务设计。
- [ ] SEC-02（待执行）：在隔离环境收集 CSP Report-Only 违规，检查图片回退、上传预览和 PDF 下载；评估后再决定是否及如何收紧策略，不直接修改线上策略。
- [ ] COMPAT-01（待环境）：Excel/WPS 实际另存工作簿兼容与导入回滚测试；使用临时文件及隔离库，前端导入入口保持现状。
- [ ] LINUX-01（待环境）：真实 Linux 镜像构建、专属依赖与系统包漏洞扫描；本机无 Docker，不能用 Python 依赖审计或 Dockerfile 静态检查替代。若使用远端环境，先单独确认权限，不重建现有服务。

### 发布事项（全部等待授权，当前暂停）

- [ ] PUB-01：将已核实的 65 张发布图片纳入 Git/LFS，并按实际引用检查实体与路径。本机当次预检的 93 个图片引用无缺失；正式发布前重新核验，不把固定数量当作永久基准。
- [ ] PUB-02：单独确定源 Excel、PSD、原图与压缩包的忽略/独立备份规则；保留用户文件，不整目录暂存、不删除源资料。经授权后只提交确认过的代码、文档、测试与发布图片。
- [ ] PUB-03：另行确认 Git 提交、是否推送及 NAS 发布；NAS 先核验目标/数据卷并备份，再按已确认的开发阶段本机全库覆盖规则执行，记录新版图片和 PDF 下载验收。当前不提交、不推送、不部署。
- [ ] PUB-04：ECS 目标地址、登录方式、数据卷和维护窗口待确认，独立验收，不从 NAS 成功推定 ECS 成功。

### 当前基线与执行顺序

- 最新已记录完整回归为 331 项、14 个子测试；随后发布工具专项 9 项通过，存在重叠，不相加成完整套件数量。本次文档更新没有重跑测试。
- 本机数据库迁移为 `20260911_0028`；NAS 已发布基线仍为 `fa1008e`。
- 资源按文件版本化：`admin/admin.css`、`admin/catalog-v2.js`、`js/catalog-marketplace.js` 为 `20260911-3`，本轮其他已更新入口资源仍使用 `20260911-2`，不要求无修改文件统一升版。
- 执行顺序：E2E-01～03 与 UI-01 → PDF-02～03 → REL-02～04 → 文档归并；P2 按风险和环境排期。PUB 系列在用户重新授权前保持暂停。
- 下方功能分项是范围说明和阶段证据，不另建重复待办；历史章节中的“当前/本轮”仅指当时检查点，不能覆盖本表。

## 今日新增需求合并：实施范围

- 已发布基线：`fa1008e`，NAS 基础部署验收通过。
- 以下七项已进入本机实施阶段；分类管理已在本机实施，不能与已发布功能混淆。
- 用户已下达“开始执行 / 继续执行”：本轮修改本机代码和文档，并备份后迁移工具/附件分类；未修改历史业务快照、未提交 Git、未部署 NAS。
- 保留既有完成记录、验收证据及未完成的安全任务。后文为历史记录，当前新增需求以本节为准。

## 新增功能与数据规则（本机实施中）

### 1. 工作台 PDF 导出语言选择

- [x] 分享记录、询价单、报价单导出 PDF 前，弹窗选择“中文版 / 英文版”，确认后生成。
- [x] 语言选择只影响本次导出，不改变界面语言、原记录、历史快照、币种、金额、数量和排序。
- [x] 取消不生成文件；生成中防重复点击，失败可重试。
- [ ] 实际后端六种下载及历史译文回退验收见 PDF-02～03；语言交互和样稿排版已完成，见 PDF-01。

### 2. 普通用户有效分享上限

- [x] 普通账户最多拥有 **10 份有效分享配置单**；业务员、管理员不受此创建限制。
- [x] 仅未关闭且未过期的分享占名额；一份分享码计一份，不按其中设备数量计数。
- [x] 个人中心及创建分享处显示“有效分享：7/10”。
- [x] 满额时停止创建，征求用户许可后跳转个人中心分享管理；取消留在原页，购物车内容保留。
- [x] 删除分享单时，同步关闭分享码并释放名额；工作台保留已关闭历史记录，不删除关联询价、报价或快照。
- [x] 恢复记录不自动开启分享；重新开启需检查权限、有效期及剩余名额。
- [x] 新旧创建入口及重新开启入口统一服务端限额校验，防止并发或绕过前端突破上限。
- [x] 既有超额账户不自动关闭记录，只限制新增和重新开启。
- [x] 工作台分享记录初次进入及“重置”后默认筛选“有效”，仍可主动切换全部、已关闭、已过期。

### 3. 附件与维修工具分类

- [x] 每个附件、每个维修工具必须且只能归属一个类别；新增、编辑均有前后端必填校验，旧新增接口不能绕过。
- [x] 附件初始分类：**测试线、油管、适配器、插头、接头、其他**。
- [x] 维修工具独立类别已由用户确认，按序为：**维修工具、测量工具、拆装工具、其他工具**；不套用附件类别，“其他工具”为兜底类。
- [x] 复用现有目录分类结构，以稳定分类 ID 关联中英文名称；工具与附件类别互相隔离。“全部”仅为筛选入口，不是可保存的分类。
- [x] 工作台与用户端均可点击分类标签快速筛选，默认“全部”，支持叠加编号/名称搜索，空类别仍可选择。
- [x] 筛选不写入购物车；沿用原有全列表客户端筛选（没有分页状态，无需新增分页）。中英文类别、手机布局、选中和空结果已验收；后台搜索结果中禁用排序，避免误排隐藏条目。
- [x] 历史条目先生成分类映射预览，不能可靠判断的建议归入“其他”待复核，不直接猜测覆盖。
- [x] 迁移仅更新未删除目录项目的分类 ID 和版本，保留价格、数量、权限、原始排序及历史业务快照；长期类别新增、改名、停用规则另行确认，当前固定类别不能通过旧接口改名或删除。

## 界面与交互调整（本机实施中）

### 4. 工作台在浏览器新标签页打开

- [x] “管理后台 / 业务员工作台”入口改用浏览器原生新标签页，不使用独立弹出窗口，保留原页面。
- [ ] 核验登录态、权限和原页面购物车状态；后台内部编辑、确认弹窗保持不变。

### 5. 补齐“查看目录”入口

- [x] 用户端维修工具、设备附件页面标题右侧增加“查看目录”按钮。
- [x] 与设备页面样式、文案及行为一致，点击展开左侧产品导航，不打开工作台或新标签页。
- [x] 保留筛选、搜索、数量及购物车状态，避免重复绑定事件或产生重复导航栏。

### 6. 修复设备目录关闭按钮

- [x] 复现“产品导航 → 设备目录”右上角关闭按钮交互显示异常，再定位修复，不预设具体根因。
- [ ] 检查默认、悬停、按下、键盘焦点、图标及点击区域。
- [x] 隔离浏览器验证关闭、Escape、遮罩、滚动解锁、导航状态和焦点恢复，覆盖 1440px / 390px；真人设备触控仍列入最终人工复验。

### 7. 报价按钮样式与购物车底部布局

- [x] **只修改两处“联系销售获取报价”按钮**：设备选配摘要卡片及购物车底部。
- [x] 两处统一蓝底白字、圆角矩形；计划默认采用 `#2563EB`、8px 圆角，悬停及按下加深。其他按钮样式不变。
- [x] 购物车顶部只保留标题和关闭按钮。
- [x] “导出 PDF”和“分享”移动到固定底部操作栏第一行，左导出、右分享，等宽并排，保留次级按钮样式。
- [x] 第二行放置占满宽度的“联系销售获取报价”按钮。
- [x] 手机端同样保持两行，保留文字、适配安全区域；列表末项不能被操作栏遮挡。
- [x] 保留原按钮 ID、事件、登录检查、禁用规则及业务流程，不复制按钮或重复绑定。

## 新增需求执行顺序与验收边界

上述功能实现阶段已完成本机检查点；后续按顶部“当前待办总表”执行：隔离环境真实后端端到端验收 → PDF 下载与回退 → 发布包异常/恢复 → 文档归并 → 等待发布授权。不会重复实施已完成的按钮、配额或分类功能。

继续沿用原项目 UI 规范。已有三角色真实流程、NAS 实际 PDF 下载、图片 Git/LFS 管理及安全专项未完成项继续保留；不因此次需求合并自动标记完成。

## 本轮实施与验证（未发布）

- 已完成代码：三个工作台 PDF 入口语言弹窗、有效分享配额及删除关闭联动、分享默认有效筛选、新标签页工作台入口、工具/附件查看目录入口、关闭按钮网格与焦点修复、两处蓝色报价按钮及购物车底部双行布局。
- 分类管理已本机实施：[分类映射与执行记录](2026-09-11-category-mapping-preview.md)。用户确认四类并要求继续后，32 项未删除工具分为维修 9、测量 12、拆装 6、其他 5；7 项附件归入其他，4 项已删除附件未移动。0028 迁移前备份 `backend/migration-backups/boten-20260911-045525-422094.db`；29 张表摘要比对通过（options 排除获准变化的 category_id/version），完整性与外键检查通过，API ready 正常。验证摘要保存在忽略目录 `tmp/category-migration-verification.json`。
- 分类阶段验收：全套 pytest **324 passed、14 subtests passed**（两条现有依赖弃用警告）；`tests/browser_catalog_categories.cjs` 验证 1440/390 宽度、两类目录、分类与搜索组合、空结果、英文类别和搜索保留、必选下拉框。截图已检查，新增控件沿用 Web Interface Guidelines 与原 UI 规范；JS 语法、`git diff --check` 通过。浏览器使用隔离接口数据，未创建真实商品或修改购物车；真实角色业务操作仍保留为后续验收。
- 报价导出只投影不可变业务快照中的译文，保留币种、价格、数量、排序及销售自定义名称；缺少译文的历史记录保留原文，不自动猜译或读取当前目录覆盖。此类记录的完整英文覆盖仍待复核。
- Python 全量回归：322 passed、14 subtests passed，2 条测试依赖弃用提示。随后新增新旧三个分享创建 API 满额拦截测试，与删除/恢复生命周期测试共 2 项通过；专项限额、导出语言与前端契约共 63 项通过。
- 隔离浏览器测试：1440px / 390px 目录关闭与焦点恢复；工具和附件目录按钮保留搜索；购物车双行按钮布局、列表末项不被底栏遮挡；满额取消后保留购物车，未满显示 7/10；新标签页继承会话且保留原页；六种 PDF 语言请求、取消及防重复触发通过。全部业务写请求被隔离拦截，未生成真实分享、询价或报价。
- PDF 技能验收：生成分享/询价/报价中英文各一份，共 6 份、12 页，逐页渲染检查 Logo、一级标题、分类标题、编号独立列及页眉页脚，未见裁切或重叠。文件位于忽略目录 `tmp/task-plan-pdf/`；属于样稿，不替代真实账号和 NAS 下载验收。
- UI 规范检查影响：保留原设计体系，仅两处报价按钮使用用户指定蓝色；语言选择采用原生可关闭对话框；菜单关闭恢复焦点，隐藏按钮不进入 Tab 循环，手机不主动聚焦备注输入框。
- 该阶段本机 API 已重新载入代码，`/api/v1/ready` 返回 ready；当时资源版本为 `20260911-2`，后续部分文件更新至 `20260911-3`，以顶部文件基线为准。本机首页为 http://127.0.0.1:8081/ 。
- 剩余项统一见顶部任务表。三角色验收指隔离数据库上的真实后端浏览器流程，不需要写入真实客户记录；PDF 异常重试已补验，不再列为未实现。本机实现不代表 NAS 已部署。


## 已发布基线与历史执行记录

最新部署进展：`fa1008e` 已发布至 Albert-Nas，数据库及容器检查通过，NAS 151 个公开资源哈希全部匹配。真实登录态 PDF 下载与角色流程仍待验收，详见 [NAS 发布记录](2026-09-11-nas-release-fa1008e.md)。以下“未部署”描述保留为此前阶段记录，以本段为当前状态。

保留现有设计和权限，实施下列已汇总任务。安全问题的详细依据及验收清单见 `2026-09-10-task-plan.md`。完成本机验收后再按用户确认执行 Git 和 NAS 重建。

当前执行约定统一见 [项目执行规则](project-execution-rules.md)。用户已授权执行本机开发与启动服务，并追加授权更新项目文件及 Git 提交；本次检查点包含实现、迁移、测试和文档，不包含推送或远端数据库覆盖、部署。

## 2026-09-11 新增任务合并：个人中心、客户信息与 PDF

以下为本轮最新状态；后面的历史记录不表示本轮已提交或已部署。

- [x] 个人中心桌面端标题与输入框同排，移动端上下排；移除个性签名输入，保留历史数据库字段；地址归入联系方式并独立保存。
- [x] 资料接口仅更新实际提交字段，地址保存不覆盖姓名、性别、生日或签名；保留版本冲突保护及未保存输入。
- [x] 我的分享、询价、报价增加搜索、筛选、分页和隐藏/恢复；隐藏仅影响本人的列表，不删除业务快照。分享支持关闭/重新开放，禁止恢复管理员关闭或已过期的分享。
- [x] 报价客户信息合并为弹窗：选客户自动填姓名、电话、邮箱、地址，可手动修改；取消不影响报价，应用仅更新本次报价，不修改账户。主编辑区保留简要预览，把空间留给填价列表。
- [x] PDF 的设备、维修工具、设备附件一级标题使用一致的字号与加粗样式；编号独立成列，不恢复分组小计，不改变金额计算。
- [x] 分享/询价到报价传递分类 ID、中英文名称及排序；按设备和分类聚合。旧草稿仅从不可变来源快照补齐缺失元数据，已发送报价不改写。
- [x] 隔离浏览器用例通过：桌面及 390px 手机布局、地址局部保存、隐藏/恢复、客户弹窗取消/应用、手动客户禁止直接发送；未写入真实业务数据。
- [x] 全量回归 315 项、14 个子测试通过（288.65 秒，2 条测试工具弃用警告）；之后补充的语言/快照恢复修复及前端/PDF 专项 67 项通过，浏览器用例复跑通过。该 67 项与全量有重叠，不相加作为独立用例总数。
- [x] 中英文询价/报价 4 份样稿共 29 页完成渲染检查，覆盖同型号双设备、多分类、工具附件和跨页。修复基础供电排序及英文人民币报价字段标题；未见裁切或重叠。样稿为隔离测试数据，不能替代真实三角色发送验收。
- [x] 本机 API 已重启，0026→0027 自动升级，ready 与首页验证通过。升级备份 `backend/migration-backups/boten-20260911-000827-023605.db`；升级前后 users=8、products=7、options=118、saved_configs=16、commerce_shares=8、customer_inquiries=3、commerce_quotes=4、quote_revisions=0，完整性 ok、外键检查为空。
- [x] 只读加载现有 4 份报价成功，236 条草稿配置可读取分类名称；没有写回历史报价、发送记录或账号信息。
- [ ] 真实登录账户的管理员/业务员/客户三角色业务验收；本轮不会为了验收创建、发送或删除真实报价。
- [ ] NAS 镜像构建、数据库覆盖、Logo 与图片同步验收仍属于独立部署任务，不以本机测试替代。

设计依据：沿用 `website-design-system.md` 与用户指定的 `web-design-guidelines`，保留既有品牌色、字体、组件体系；新增表单标签、焦点提示、异步状态和弹窗焦点恢复。

本轮审查记录见 [界面与数据链路验收](2026-09-11-ui-review.md)。运行入口：`http://127.0.0.1:8081/`、`/account/`、`/admin/`。新 API 仅本机回环监听；局域网/NAS 服务并未因此更新。

## 当前验收状态说明

- 勾选表示该条完整范围已有验证；未勾选不等于尚未实现，部分条目包含本机实现和 NAS 验收两层要求。
- 历史检查点回归：311 项、14 个子测试通过（152.73 秒）；本次最新验证结果见上方新增任务合并节，不用历史数值替代当前结果。
- 中英文询价 6 页、多设备报价 4 页已完成样例逐页验收；真实购物车与报价分组排序的一致性仍需浏览器核验。
- 用户本轮已授权更新文档并提交一次 Git；此授权不包含推送远端或 NAS 重建。本机没有 Docker，不能用静态 Dockerfile 测试代替实际构建。

## 历史未完成事项（旧提交检查点，非当前总表）

- 用户已确认开发阶段本机为准、全库覆盖远端；同步前必须备份远端并停止写入，图片随库发布。目标 SSH/容器管理入口及 ECS 具体目标仍需核实；确认策略不代表同步已执行。

- 新增要求：每次开发发布同步静态资源、本机全库和对应图片至所选 NAS/ECS。全库覆盖方式已确认，待实现差异预览、备份、传输、引用校验与失败恢复；当前没有自动同步机制。已发现 tb/configpic 中存在未跟踪图片和源资料，需区分实际发布图后纳入 Git/LFS，不能整目录盲目提交。部署指引已要求同时重建 web/api，避免 Logo 与页面资源停留在旧镜像。

1. 浏览器验收：客户资料一键填充（含非空电话/地址）、手动修改保存、三角色分享至收件/PDF 全流程、全部询价状态颜色及已分配姓名；使用隔离测试数据，不能仅凭 API 测试勾选。
2. Excel/WPS：真实另存工作簿兼容验证；后台导入入口保持原有关闭状态。
3. 请求模型：报价条目目前为字典白名单及部分嵌套显式模型，尚未完成计划要求的全行显式模型收敛。
4. 旧目录：已补关键更新的版本保护及字段边界，但旧接口尚未全部转交统一目录服务；审计、图片引用及旧客户端兼容仍需整体核验。
5. CSP：仅静态兼容评估，尚未运行 Report-Only 浏览器违规收集或上线收紧策略。
6. Linux/NAS：构建镜像、Linux 专属依赖和镜像漏洞扫描、迁移前备份及升级后数量核对、敏感路径拒绝、全部图片和 PDF Logo 验收；重建须另行确认。
7. 最终人工验收：键盘操作、三角色及响应式/缩放复验；当前提交是阶段检查点，不宣称全部任务完成。

## 部署与安全

- [ ] Web 静态资源白名单，禁止发布 Excel、PSD、ZIP、源 PDF；验证现有图片可用。
- [ ] API 镜像包含 tb/site/BOTEN.png，验证 NAS PDF 图片 Logo。
- [ ] 统一 Alembic 启动迁移，覆盖空库、历史库和版本漂移，升级前备份。
- [ ] 数据库就绪及同源页面、API、图片冒烟检查。
- [ ] 请求体、嵌套业务数据和报价字段边界。
- [ ] Excel ZIP/XML 解析资源上限及事务回滚。
- [ ] 旧目录接口统一校验、版本冲突和图片引用规则。
- [ ] 登录字段上限、限流容量及过期回收。
- [ ] 敏感响应 no-store、图片协议校验、CSP 兼容评估、生产 CORS 校验。
- [ ] Python 3.12 依赖锁定与 CVE 审计。

## 询价界面

- [ ] 已分配显示姓名；缺失姓名回退为已分配。
- [ ] 已转报价蓝色，未转报价橙色；已报价绿色，未报价黄色。
- [ ] 询价列表报价栏隐藏报价人，保留份数。
- [ ] 询价详情删除关联分享码展示模块，保留来源数据。

## PDF 和报价客户信息

- [x] 询价 PDF 隐藏文档信息；页眉右对齐公司、询价创建时间、询价单号。（当前样例中英文共 6 页逐页验证）
- [x] 询价 PDF 页脚左网址、中页码、右生成时间。（同上）
- [ ] 报价客户固定姓名、电话、邮箱、地址，一键从客户读取并允许修改。
- [ ] 地址向后兼容迁移，报价保存客户快照，历史发送版本保持独立。
- [x] 报价 PDF 隐藏文档信息；页眉右对齐公司、报价创建时间、报价单号。（中英文多设备样例共 4 页验证）
- [x] 报价页脚左公司地址、中页码、右网址。（同上）
- [ ] 中文公司地址：江苏省昆山开发区马塘路108号。
- [ ] 英文公司地址：No.108 Matang RD, Kunshan Development Zone, Suzhou, Jiangsu, China 215333。
- [ ] 设备配置按购物车类别及项目排序聚合，各设备保持独立。
- [ ] 删除报价分组小计展示，单价、行小计及总额计算保持不变。

## 验收

- [x] 原有回归和当前已实现的数据、安全边界测试通过（311 项、14 个子测试；不代表尚未实现的范围已覆盖）。
- [ ] 中英文多页 PDF 全页渲染验收，检查长地址、Logo、分页及金额。
- [ ] 三角色分享、询价、报价、发送和客户导出全链路验收。
- [x] 更新文档、记录实测结果和剩余事项。
- [x] 用户明确授权本次阶段 Git 提交；仅提交源码、测试和文档，排除数据库、源资料、截图和临时文件。不代表最终业务验收完成。
- [ ] 用户确认后 NAS 重建及完整冒烟验收。

## 执行记录

- 客户四字段历史快照专项扩展通过（44.83 秒）：发送报价后修改草稿姓名、邮箱、地址并明确清空电话，保存结果保持这些修改；客户读取已发送版本时，四字段均保持发送前原值。使用临时测试数据库，真实客户信息未修改。尚不替代浏览器一键填充及完整发送界面验收。

- 当前本机业务库只读复核：integrity_check=ok、foreign_key_check=0；users=8、products=7、options=118、saved_configs=16、commerce_shares=8、customer_inquiries=3、commerce_quotes=4。仅记录现状，不以较早记录的业务数量为基准回滚或删除新记录。

- 本机白名单图片全量只读验收：枚举 tb 下现存 133 张 PNG/JPG/JPEG/WebP/SVG/ICO 图片，逐项经 8081 发起 HEAD 请求，133/133 返回 200 且 Content-Type 为 image/*，无失败。该结果证明本机白名单未屏蔽现有静态图片，不代表 NAS 镜像已构建或通过验收。

- 分享到报价的三角色 API 串联补齐并通过（45.50 秒）：业务员分享设备及备注、客户预览/导入购物车/创建询价、业务员分配及转报价、管理员查看、发送后客户读取并导出 PDF。扩展测试的首次失败是清理阶段遗漏分享和配置的外键引用，已修正清理顺序；业务代码及真实业务数据未改动。仍不替代三角色浏览器全流程验收。

- 浏览器客户资料疑点复核：HTTP 返回 admin.js 与本机源文件完全一致，手动资料提示文案按接收客户状态动态切换，并非缓存异常。Chrome 实际截图确认报价 BTQ-20260910-0003 的手动资料弹窗姓名、邮箱、电话均预填；浏览器文本/DOM读取未反映邮箱电话内容，不能据此判定数据为空。已取消退出，未保存或发送，不修改正常功能。此证据仅覆盖打开预填，不替代修改保存及客户收件全链路验证。

- 收尾核验：此前完整回归已结束，309 项及 14 个子测试通过（229.24 秒，2 条测试依赖弃用警告）。随后本地服务专项 2 项通过，覆盖静态白名单、超限/非法请求长度拒绝以及伪造转发头过滤。已核对进程后重启本机 8081 服务加载过滤规则；首页、后台、Logo 和 ready 返回 200，数据库及源表返回 404。未重建 NAS，未提交 Git。

- 发现并修复本机静态服务泄露：原通用 http.server 对 /backend/boten.db HEAD 返回 200。已改用受限本地服务，静态文件白名单、禁止目录列表及越界路径，并限制代理请求体。本机实测首页/后台/Logo 200，数据库/源表/目录列表 404，同源 GET /api/v1/ready 正常（HEAD 405 为后端未提供此方法）。静态专项通过；最新代理转发头过滤尚需重启加载。NAS 未改动。

- 中英文多设备报价样例各 2 页，全部 4 页渲染检查通过：两台设备独立分组，乱序输入规范为 A1/A2/B1/B2；工具、附件置后，客户四字段和长地址完整，行小计保留且无分组小计，总计 CNY 3,265.00 与算式一致。使用合成验收数据，未写业务库；不替代真实购物车到报价的完整浏览器验证。

- API Dockerfile 接入已验证 Python 约束文件及 pip check，防止重建时共同依赖自由漂移；部署契约/冒烟脚本测试 3 项通过。本机未发现 docker 命令，实际 Linux 镜像构建和专属 extras 锁定未完成，未执行 NAS 重建。

- 完成 CSP 静态兼容评估，详见 csp-compatibility-review.md：现有内联 onerror 和后台错误脚本使直接启用严格 script-src 不兼容；没有修改运行策略。Report-Only/实际浏览器违规收集仍未执行，不认定 CSP 已上线。

- 三角色 API 串联专项补齐并通过（51.03 秒）：客户创建询价、业务员分配/联系/转报价、管理员查看、业务员发送、客户读取并导出 PDF，保留第二业务员独立报价和重放检查。新增测试曾误用客户 URL，已按实际 /customer/me/quotes 修正；未改业务路由或真实数据。此项是临时数据库 API 验证，不代表三角色完整浏览器和分享链路全部验收。

- 类别版本及字段边界合并后完整回归 308 项、14 个子测试通过（224.59 秒）。本机 API 重启载入，ready 正常、首页 200、产品 7 台；无 Git 提交或 NAS 变更。整体验收仍未完成。

- 使用已登录管理员浏览器查看设备目录：7 台设备可见，CR1016 分栏式编辑器正常打开，基础资料/外观/配置/价格标签可见；点击取消后弹窗关闭，未保存测试数据。该页面实际使用分栏编辑器，不能据此认定旧编辑器提交已完成浏览器验证。最新完整回归正在运行。

- 旧类别创建/更新补齐与新版一致的标题 200、描述 5000 字符限制，排序限制在 SQLite 整数范围并保留负数；9 项边界测试通过，省略更新字段仍不覆盖原值。完整回归、浏览器验证及重启待执行。

- 旧配置类别更新加入版本条件和递增，创建/列表响应返回版本，旧类别编辑器提交读取版本。类别创建、成功修改和旧版本 409 纳入目录 CRUD 专项，通过（50.33 秒）。完整回归、浏览器验证和服务重启尚待执行。

- 设备旧接口版本保护及最新 ZIP/价格边界完整回归：299 项、14 个子测试通过（233.24 秒）；前端脚本语法和 diff 检查通过。本机 API 已重启，ready 正常、首页 200、产品接口 7 台。后台应刷新以取得设备版本字段。未执行 Git/NAS；其余计划验证仍未完成。

- 独立配置备注/覆盖价接口加入设备版本比较与递增，写入共用立即事务。双语备注、取消映射后恢复及旧版本拒绝等筛选测试 5 项通过（45.68 秒）。设备各旧写入口已补版本机制，但仍需完整回归与浏览器验证；类别等其他目录写入口尚未全部收敛，本批未重启。

- 独立设备映射替换接口加入设备版本比较与立即事务，旧版本返回 409；直接调用测试同步传入当前版本。atomic/mapping 筛选测试 2 项通过（56.49 秒），覆盖接口成功及过期版本。仍需完整回归覆盖全部直接调用和映射恢复场景，独立覆盖接口尚待处理，本批未重启。

- 独立颜色替换接口加入设备版本比较，检查与颜色替换共用立即事务；成功版本递增，旧版本返回 409。与 PATCH/完整保存联合专项通过（42.67 秒）。当前前端使用完整保存，不需要增加新交互；独立映射及覆盖接口、完整回归和重启仍待处理。

- 旧设备基础 PATCH 写入加入版本比较与递增，缺失/过期版本返回 409，未知设备保持 404。与完整配置 PUT 共用设备版本；真实接口专项覆盖 PATCH 缺版本、成功、过期及后续完整保存，通过（46.52 秒）。颜色、映射等独立接口尚未完成同等保护；完整回归和服务重启待执行。

- 旧设备完整配置 PUT 保存加入必需版本、立即事务和原子比较递增；详情响应返回 version，原前端提交打开时版本。真实接口验证成功保存版本加一、旧版本返回 409、无效配置不覆盖描述，专项通过（41.18 秒）。缺版本请求现在返回 422，页面需刷新；独立 PATCH/颜色等旧接口尚未全部覆盖，完整回归及重启待执行。

- 旧设备创建/修改的人民币、美元基础价补齐非负及 SQLite 整数范围，排序允许负数但拒绝整数溢出；省略更新字段仍不覆盖现值。产品价格专项累计 16 项通过。完整回归及重启未执行，其他旧接口版本保护仍待补齐。

- ZIP 白名单加入后，本机实际导出模板只读内存预检通过：7 设备、75 配置、8 电机价格、0 参数，四表无错误。另补齐旧设备电机价格/配置覆盖价非负及 SQLite 整数上限，保留 null 清除覆盖价和零价格语义；7 项专项通过。本批完整回归、重启及 Excel/WPS 实际另存兼容仍待完成。

- Excel ZIP 内容加入明确白名单，拒绝宏、嵌入对象、异常路径和其他无关文件；同时核对原始 ZIP 名称，避免 Windows 读取器规范化反斜杠后绕过检查。41 项解析测试、4 个子测试通过，标准元数据/样式/主题文件保持支持。实际 Excel/WPS 另存文件的兼容验证及完整回归仍待完成，本批尚未重启。

- 最新报价字段校验合并后，完整回归 271 项、14 个子测试通过（215.10 秒）；本机 API 已重启载入，ready 正常、首页 HTTP 200、产品接口 7 台。仅剩测试依赖的两条弃用警告。仍未完成全部计划验收，不代表 NAS 已更新。

- 使用当前本机 CR1016 目录全部可选项生成中英文询价验收样例（各 3 页），逐页查看全部 6 张渲染图：图片 Logo、公司/询价时间/编号右对齐、文档信息隐藏、编号独立列、跨页表头以及页脚网址/页码/生成时间位置正常，无明显重叠或截断。仅生成测试文件，未写业务数据库。多设备报价和三角色验收仍未完成；完整回归进程另在运行。

- 报价实际 POST 接口边界验证通过：顶层未知条目字段、规格内未知字段、可用性明细对象均返回 422，逐次核对报价表记录数不变。1 项测试、3 个子测试通过（41.92 秒），使用临时测试数据库。本批完整回归和重启仍待完成。

- 报价嵌套校验加入设备规格显式模型（key/label/value，拒绝额外字段）及可用性明细字符串列表，限制条目数和文本长度；核对询价来源与前端生成结构一致。30 项字段/数值测试通过，涵盖嵌套对象、错误类型、超长键和未知字段。完整业务回归及本批重启尚待执行。

- 报价请求复用入库字段白名单，对未知条目顶层字段明确拒绝，不再静默忽略。字段与数值专项 22 项通过，现有报价业务测试 5 项通过（71.04 秒）。嵌套规格/可用性明细模型及最终完整回归仍需完成，本批代码尚未重启载入。

- 报价条目归一化改为显式字段白名单，未知顶层附加字段不再随报价写入；保留分组排序、设备规格、可用性及价格字段，金额算法不变。新增字段过滤测试与数值边界专项共 20 项通过。嵌套行模型、API 未知字段拒绝及完整保存/历史兼容回归仍待完成，本批未重启。

- 测试数据恢复后全量回归 259 项、11 个子测试通过（229.90 秒），仅两条测试工具弃用警告。本机 API 已重启载入最新代码，启动完成、ready 正常，产品接口 items 返回 7 台，主页 HTTP 200。该结果不替代三角色/PDF 全页及 NAS 验收，未执行 Git 或 NAS 重建。

- 全量回归发现新增成功导入测试未恢复共用测试数据库，设备名污染导致后续 PDF 断言失败（258 通过、1 失败）。已为两项成功导入测试注册清理，恢复设备、配置、电机价格与参数测试数据；没有放宽 PDF 断言。完整回归已重新启动，结果待确认；本机运行服务尚未重启。

- 补齐电机价格和设备参数导入对设备版本的联动：在同一事务末尾按设备去重递增版本；仅电机、仅参数及三表同时更新均验证递增一次。导入专项 4 项、3 个子测试通过（47.12 秒），包含旧编辑器冲突和事务回滚。最新改动尚待全量回归及服务重启，不代表所有旧目录接口或 NAS 验收已完成。

- Excel 设备/配置表导入现在同步递增对应记录版本，避免导入前打开的配置编辑器覆盖新值。新增真实 API 测试验证导入成功、设备及配置版本递增、旧配置版本保存返回 409 且导入值保留；与导入事务回滚测试合计 2 项通过（42.40 秒）。尚未覆盖电机价格/参数单独导入的设备版本联动，最终全量回归及服务重启仍待执行，未改动 NAS。

- 导入预检补齐设备/电机映射及参数归属验证，普通配置不能当作电机价格导入，未知参数不再静默忽略。关联错误专项通过；本机实际导出模板经解析与完整预检返回 valid=true、四表无错误（只读内存验证）。最终全量回归及重启待执行。

- 预览与提交共用行校验，新增缺列、重复记录、价格/排序整数范围检查，提交在备份和写库前拒绝无效报告。API 专项验证负价格预览无效、提交 400 且不创建备份；事务回滚测试绕过预检以保留写入后失败覆盖。2 项通过（45.82 秒）。电机组合等更细业务校验仍需统一，完整回归未执行。

- 新增正式导入事务专项：模拟解析后的工作表，首张设备表已更新后，第二张配置表未知编号触发 400，验证首张表姓名及价格等字段全部回滚。真实测试数据库/API 流程通过（42.82 秒）；该测试隔离了解析器和备份，不代表 ZIP/XML 或备份实物验证。用户数据库未改动。

- 本机真实目录模板内存往返验证通过：7 台设备、当前旧目录模板包含的 75 个配置条目，编号/分类/中英文名称/描述/备注/价格/启用字段逐项一致；4 张表成功解析，8 条电机价格行保留。当前设备参数为 0 行，不能据此证明非空参数兼容；未保存工作簿或写入数据库，Excel/WPS 另存版本和正式导入事务仍待验证。

- 导入行现在按单元格实际列号归位，保留空列并支持乱序 XML；重复列引用明确拒绝，省略坐标的单元格按前一列推断。Excel 专项 29 项、4 个子测试通过。未改动源表，实际模板/完整回归和本批重启尚待完成。

- 导入坐标解析不再通过“抽取字母”猜测列，明确拒绝 A1B2、A0、负行号、区域引用等畸形坐标；正常多字母列和大小写保持兼容。Excel 解析/XML 专项 26 项、4 个子测试通过，未改动用户源表。完整回归和实际模板验收仍待执行。

- Excel 解析修复共享字符串负索引误读末尾内容的问题；负数、越界、空值和非整数引用现在明确拒绝，正常引用保持原值。解析/XML 专项 15 项、4 个子测试通过。仅修改程序与单元测试，未改动用户 Excel 文件；ZIP 白名单、完整回滚及实际模板兼容验收仍未全部完成。

- 233 项全量回归通过的代码已重启载入本机 API：启动完成、ready 正常、产品接口返回 7 台、主页 HTTP 200。仍未进行 Git 提交或 NAS 部署；后续变更需重新验证，不能将服务启动视为全部任务完成。

- 业务请求公共模型及购物车条目引用拒绝未知字段，新增专项通过；合并近期名称清洗、旧目录边界/去重后全量 233 项、8 个子测试通过（217.27 秒），仅测试工具两条弃用警告。报价条目内部白名单等尚未完成，不能据此认定请求边界任务全部完成；本批代码待重启载入。

- 旧配置条目创建和修改增加编号重复检查，检查与写入使用同一立即事务，重复编号返回 409；与新版一样按去首尾空白、不区分大小写匹配。旧目录 API 回归包含重复创建、缺失版本、过期版本和保存结果核查，通过（40.21 秒）。尚未完成所有旧接口整合和最终全量验收。

- 旧配置创建/更新补齐与新版一致的文本长度上限，价格拒绝负数，价格/排序/版本限制在 SQLite 有符号整数范围内，避免绑定溢出。19 项模型边界测试通过，包含省略字段不覆盖原值；后端完整回归与重启待完成。

- 旧配置条目创建/更新接入共用编号与名称清洗函数；共用名称函数将换行、制表符和连续空白规范为空格，移除重复编号后为空的中文名称被拒绝。新增名称专项 3 项及旧目录 API 回归 1 项通过。未批量修改历史数据库；完整回归及重启仍待执行。

- 目录版本保护合并后完整回归 205 项、8 个子测试通过（214.87 秒）；客户资料 Node 行为测试、脚本语法及 diff 空白检查通过。本机 API 已重启载入最新修复，ready 与首页 HTTP 200 验证通过；未提交 Git 或重建 NAS。其余未验收项保持未完成。

- 旧配置条目 PATCH 已加入原子版本比较并递增版本，目录列表/创建/更新响应携带版本；旧编辑器提交该版本并显示冲突提示。缺失版本的旧客户端返回 409，需刷新页面后编辑，不再静默覆盖。真实 API 回归覆盖缺失版本、成功保存、旧版本重复保存及数据库值保持，共 1 项通过（42.49 秒）。旧产品、类别等其他接口及统一字段规范仍待完成；后台改动尚未重启载入。

- 目录并发审查发现新版条目更新未检查 SQL 条件更新行数，现已在零行更新时抛出版本冲突并回滚，避免未写入却返回成功。新增模拟竞争专项 1 项通过，既有目录相关回归 16 项通过（50.15 秒）。旧接口统一校验/版本改造尚未完成；本次后台代码待下次服务重启载入。

- 全新 Windows Python 3.12 虚拟环境实际重装锁定依赖成功，pip check 无冲突；就绪、数据库升级、报价请求边界与 PDF 分页共 22 项通过。运行中服务未切换到临时环境；Linux/NAS 镜像构建、系统包与专属依赖仍待验证。

- 一键填充浏览器复验：历史询价绑定管理员账号，精确客户查询按现有权限规则不返回该账号，页面明确提示客户资料不可用。通过客户选择器选取有效客户后，姓名/邮箱自动填充成功；临时地址被再次填充恢复为客户现有空地址，错误提示清除。已取消并确认放弃全部临时修改，没有保存、发送或更改账号资料。非空电话/地址及三角色保存发送链路仍需专项验收。

- 用户登录后完成管理员浏览器部分验收：两条已转报价气泡为蓝色、已报价格气泡为绿色且列表不显示报价人；询价详情无关联分享码模块。打开我的报价成功，手动修改姓名/电话及地址控件可用，取消会出现放弃修改确认，已放弃临时值，未保存或发送报价。一键填充尚未取得完整有效结果，需继续核查；缺少已分配/未报价样例，不能推断所有状态及三角色流程均已通过。

- 修复询价入口复用已有报价时客户电话被询价值覆盖的问题；客户姓名、邮箱、电话使用空值回退，保留用户明确清空的已保存值。地址通过编辑器参数传递，移除全局 DOM 补填。新增 `node tests/quote_customer_context.cjs` 行为测试，覆盖修改值、空值、历史字段缺失和询价列表缺失；测试通过，JavaScript 语法及 58 项前端契约测试通过。真实登录态流程仍待验收。

- 最新代码已重启载入本机 API，启动日志及就绪检查通过。真实浏览器匿名主页加载成功，未捕获控制台错误；目录按钮中文“查看目录”、英文“View Catalog”切换正确，点击展开产品导航。当前浏览器未登录，后台三角色验收尚待登录后进行，不能视为通过。

- 本轮报价边界修复后，Python 3.12 全量回归 204 项、8 个子测试通过（183.61 秒），仅两条测试工具弃用警告。最新代码尚未重启载入本机 API；真实浏览器、多场景 PDF 和 NAS 验收仍未完成。

- 报价请求增加项目对象类型、关联编号 100 字符限制，并在嵌套业务数据中拒绝非有限浮点数；报价总额字符串转换后也必须为有限值。请求边界、嵌套边界与错误响应合计 25 项、4 个子测试通过；全量回归正在执行。

- 继续核验：本机 8081 与 8001 均有监听进程。补齐报价历史数据中非有限排序值和超大参考价的溢出保护，避免读取报价时出现未处理异常；数量、单价和金额规则不变。`tests/test_quote_numeric_bounds.py` 共 19 项通过。该结果仅证明数值边界专项，不代表全链路验收完成。

- 已开始执行；上述复选框仅在得到对应验证证据后勾选。
- 询价列表已实现姓名、颜色和隐藏报价人；详情移除来源模块，JavaScript 语法检查通过，浏览器验收待完成。
- Web 镜像改为构建阶段过滤静态资源再复制到最终镜像；Docker 上下文排除源资料，Nginx 拒绝敏感静态路径。现有 tb 下 133 张图片均在允许范围内。
- API 镜像已补 Logo 复制。当前本机没有可调用的 Docker 命令，镜像构建及 NAS 运行验证待完成；未修改 NAS 部署。
- 询价及报价 PDF 已隐藏文档信息；新增对应页眉业务时间、编号和专用页脚，报价移除分组小计。语法检查通过，文本回归及全页渲染验收进行中；客户地址数据链路尚未实现。
- 后续进展：客户地址迁移、账号资料保存、报价地址保存读取、客户精确查询及一键填充已接入；报价地址接口回归通过。浏览器和历史发送快照专项验收待完成。
- Excel 新增压缩比、解压大小、条目数、行列和文本上限；正常与异常样例 3 项通过。
- 登录输入限制、限流容量回收及敏感响应 no-store 已实现；限流容量、过期及清除检查通过。
- 新增 /api/v1/ready 只读就绪检查，启动迁移统一及 Compose 接入待完成。
- 完整回归首轮 107 passed、2 failed；两处旧契约（来源模块、迁移头）已更新，定向复验中。完整视觉、部署和剩余安全任务未完成。
- 启动迁移已统一到 `backend.database_upgrade`，Compose 健康检查已切换 `/api/v1/ready`。新增就绪专项测试通过，覆盖缺库不创建、旧版本及缺表拒绝就绪。
- 迁移专项测试通过：0018 升级、结构超前但版本落后、重复启动；验证升级前备份、完整性、外键及保留测试数据。生产业务量核对仍待验证。
- 统一迁移后的完整回归为 104 通过、9 失败，定位为种子目录在迁移后插入，导致电机初始价格缺失；已将价格初始化移入种子事务且不覆盖现有价格，后端复验进行中。
- 电机种子初始化修复后，后端完整测试文件 48 项全部通过（157.53 秒）；仍有 Python 3.8 加密依赖弃用警告。
- 管理员创建、更新和重置密码接口已补充 1024 字符上限，新增边界及省略密码测试 4 项通过。全项目最终回归和真实浏览器验收尚未完成。
- 图片地址统一校验已接入新旧颜色和配置请求，拒绝危险协议、路径穿越及带凭据外链；图片专项 19 项通过。生产 CORS 显式校验专项 8 项通过；XML 解析器禁止 DTD，含 UTF-16 的安全测试及 Excel 原测试共 8 项通过。
- 本机 API 已重启加载当前代码，`/api/v1/ready` 返回 ready、产品接口和首页返回 200。NAS 未修改。
- 本机数据库从 0025 升至 0026，备份：`backend/migration-backups/boten-20260910-153958-265361.db`。升级前后 users=8、products=7、options=118、saved_configs=16、commerce_shares=8、customer_inquiries=2、commerce_quotes=3、quote_revisions=0；两库 integrity_check=ok，foreign_key_check 均为空。历史发送快照需专项数据验收。
- 后续完整回归 146 项通过（189.62 秒）；该次收集不含随后新增的专项测试。后续独立验证：发送地址快照保持原值 1 项、校验错误序列化及敏感错误响应 no-store 7 项、报价非有限数量与总额溢出 6 项、限流容量和过期回收 3 项，均通过。最新全量合并回归仍待执行。
- 后续 Python 3.8 全量 168 项通过；Python 3.12.14 隔离环境补齐 tzdata 后全量 169 项通过（219.91 秒），有两条测试工具弃用警告。该次收集不包含之后新增的 PDF 分页专项；分页与旧类别排序专项另有 4 项通过。
- PDF 视觉验收发现长分组强制后移造成留白，已改为超长表格自然跨页；中英文报价样稿各 3 页，6 页逐页检查未见裁切或重叠，Logo、四项客户资料、页眉页脚和 550 元总额显示正常。询价样稿和更丰富分组场景仍待验收。
- 新增 deploy/smoke.py 只读同源检查脚本；真实镜像构建及部署验收尚未执行。迁移备份与嵌套数据库已排除 Docker 上下文，迁移备份也排除 Git；容器入口脚本固定 LF。
- 依赖审计首次发现 Pillow、pip、pytest 的 41 条已知漏洞记录；隔离环境升级至 Pillow 12.3.0、pip 26.2.1、pytest 9.0.3 后重新审计为 0 条，pip check 通过。新环境完整回归 180 项、8 个子测试通过；图片解析格式限制的后续专项仍在运行。运行环境版本约束与重现说明已保存；在线本机 API 仍待切换，NAS 未修改。
