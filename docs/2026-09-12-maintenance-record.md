# 2026-09-12 本机维护记录

## 2026-09-13 增量：仪表盘与报价分类标题（本机已验收）

本段是这两项工作的最新状态；下方9月12日的提交、基线与验收描述保留为历史记录。本轮仅本机实现和隔离验证，未提交Git、未发布NAS、未改写正式业务数据。

| 工作 | 当前结果 | 验证与实施记录 |
| --- | --- | --- |
| 仪表盘扩展 | 管理员/业务员首页、待办、7/30天趋势、近期业务、目录及管理员动态；独立只读统计接口 | 后端61项及6个子测试、前端58项契约、仪表盘专项通过；见[仪表盘实施记录](2026-09-13-dashboard-implementation.md) |
| 报价分类与滚动标题 | 每台设备内分类分隔，固定栏随滚动切换设备/分类，低高度聚焦适配 | 五种尺寸专项、58项契约及完整540项布局回归通过；见[报价分类实施记录](2026-09-13-quote-sections-implementation.md) |

仪表盘新增API，但不新增数据库表或迁移；报价分类标题增量仅前端。8082仍连接NAS正式API，新仪表盘接口未在本轮发布；缺少接口时页面显示兼容提示。后续需单独确认发布，先兼容API后前端，不同步本机测试数据。


## 最新检查点：2026-09-12 文档归档与本地 Git 提交

- 授权：用户“更新 md，git”。提交前基线 `e62f24e`，本节随本轮相关代码、测试、文档纳入本地提交；以下增量中的“未提交”是历史状态，不代表本检查点状态。
- 范围：前台/个人中心/工作台适配，目录排序、语言可见性及缩略图，导航回顶部、文案与字体，摘要固定底部按钮及对应隔离回归。未修改后端数据库结构、PDF 模板或生产数据。
- 不纳入：未跟踪 Excel、PSD、原图，数据库、私有配置、临时截图/PDF。提交前索引为空；不使用全目录暂存。
- 远端：未推送、未部署 NAS/ECS、未同步数据库；NAS 产品/账号兼容合并规则仅文档更新，正式迁移与合并工具仍需单独实施和授权。
- 剩余验收：真实 iOS/Android 键盘/安全区、真实 200% 缩放、真实后端三角色全链路及远端恢复验收，不能由布局 fixtures 替代。
- 提交前复验：8 个专项浏览器脚本（排序、目录可用性、导航缩略图、概览徽章、个人中心导航、目录滚动、摘要底栏、购物车字号）全部通过；`browser_ui_responsive.cjs` 540 项布局断言通过；`python -m pytest tests/test_admin_frontend_contract.py -q`：58 passed。浏览器业务接口使用隔离 fixtures，不代表真实后端全量回归。

## 最新增量：已选配置抽屉固定底部按钮

- 小屏将整层滚动改为 `.summary-content` 正文滚动，保存到购物车与联系销售按钮固定在抽屉底部；动态视口、安全区、低高度布局及错误提示滚入可见区域已实现。按钮 ID、事件、请求参数与业务规则不变；桌面继续原摘要布局。
- `node tests/browser_summary_footer.cjs`：24 组通过，含两种语言、短/长清单、6 种尺寸、滚动前后按钮坐标、点击区域、错误提示、Escape 与焦点恢复。`python -m pytest tests/test_admin_frontend_contract.py -q`：58 passed；JS 语法通过，差异空白检查通过（已有 CRLF/LF 提示保留）。
- 实际查看 `tmp/summary-footer/` 手机与横屏截图；所有测试 API 隔离，不写真实配置或询价。真机安全区/软键盘待验收，Git 未提交，NAS 未部署。
- 本增量完成后 `node tests/browser_ui_responsive.cjs` 全站 540 项布局断言通过，中英文及 10 个尺寸，覆盖前台、个人中心和工作台；不代表真实后端全业务验收。

## 最新增量：个人中心导航与自适应排布

- 已实现：≤900px 从横向长标签改为账号摘要＋两组三列导航；桌面保留侧栏。六入口、原 Hash、主页、退出登录、未读徽章及原表单提交字段不变；修正 `aria-current="page"` 与导航焦点样式。
- 补充表单最小宽度、窄屏业务筛选及记录操作换行；修复英文 320px “更多”菜单向左越界。沿用品牌色与本地字体，无新依赖或 API/数据库变更。
- 本机测试命令：`node tests/browser_account_navigation.cjs`，两种语言、11 个视口、132 组面板检查通过；包含键盘、菜单 Escape、保存草稿/提交参数、长文本、空与失败状态。`python -m pytest tests/test_admin_frontend_contract.py -q`：58 passed；`node --check account/account.js` 通过。
- 本增量后完整执行 `node tests/browser_ui_responsive.cjs`：540 项布局断言通过，覆盖前台/个人中心/工作台的中英文与 10 个尺寸；不是完整后端业务回归。
- 证据：`tmp/account-navigation/` 前后截图已检查，详细问题位置和范围见 UI 审核记录。所有 API 由内存 fixtures 截获，未操作真实账号或业务记录；真实手机、软键盘和真实 200% 缩放待验收。Git 未提交；NAS 未部署。

## 最新规则：NAS 产品与账号本机优先，NAS 独有记录保留

- 用户已确认：部署时本机所有产品信息及用户账号新增或覆盖对应记录；NAS 存在但本机没有的记录继续保留，不清空表、不替换全库。
- 产品含设备、颜色、参数、分类、配置、工具、附件、价格、排序、映射及图片；同一账号的资料、密码哈希、角色与状态以本机为准，不复制会话。身份/编号对应不明仍需确认，不能错误合并不同人或产品。
- NAS 业务历史、归属和引用保持不变；图片路径冲突不得破坏历史实体。双端备份、隔离演练、正式应用前复查及失败回退仍为前置条件。
- 已更新执行规则、部署指南、README、今日计划及历史计划的现行规则提示，撤下部署指南中会直接恢复本机源库的旧流程。本轮仅文档修改；未开发/执行合并工具，未访问 NAS 数据库，未提交 Git、未部署。

## 最新增量：概览分享状态文字对齐

- 原因：`.mini-row span` 将状态徽章覆盖为 `display: block`，并加上 3px 顶部间距，导致文字偏上。已将规则限定为左侧说明文字，恢复徽章 flex 居中，保留颜色/字号及状态逻辑。
- `tests/browser_dashboard_badges.cjs` 修复前复现失败（徽章 display 为 block），修复后通过；覆盖 1103/1440/390/320px、有效/已关闭、徽章与行居中及原说明间距。前后截图在 `tmp/dashboard-badges/`；已检查 1103px 修复后渲染。
- `tests/test_admin_frontend_contract.py` 58 项通过。测试 API 全部隔离，未修改真实分享；未提交 Git、未部署 NAS。

## 最新增量：故障排查后的焦点与测试修复

- 已实现：小屏二级设备目录打开时一级目录设为 inert，Tab/Shift+Tab 仅在可见层循环；返回、关闭、空目录及跨 639/640px 切换恢复可见焦点。桌面继续保留双栏导航。
- 已实现：进入排序聚焦首个序号（空列表聚焦取消），插入后跟随条目；取消/保存成功返回排序按钮，保存失败聚焦重试。保存中聚焦状态提示，用户主动移至其他控件后不抢回焦点。
- 测试同步：旧“固定默认 CR1016”和“报价列表固定高度”断言改为当前语言可见首项、跨设备选项隔离、报价单一滚动正文及操作栏可达性检查；没有恢复旧行为或删除对应测试。
- 本机本轮验收：`pytest tests/test_admin_frontend_contract.py -q` **58 通过**（排查时为 56 通过、2 失败）；`browser_product_order.cjs`、`browser_navigation_thumbnails.cjs` 新增焦点/屏幕切换/空列表回归通过；`browser_catalog_readiness.cjs` 通过；`browser_ui_responsive.cjs` 中英文十种视口 **540 项断言通过**。写接口均隔离，不代表全量后端或真机验收。
- 页面标题：客户端与个人中心为 `BenchShop-博特恩检测设备`，工作台为 `BenchShop-工作台`；双语六个页面组合已验证，标题不随语言改变。
- Git/远端：未提交、未推送、未部署；未修改数据库或真实业务数据。真实 iOS/Android、软键盘、全业务状态仍待专项验收。

## 最新增量：产品导航缩略图

- 已实现：目录项及设备项文字左侧 48×48px 图片，首项/首图按当前语言和原排序选择，统一缺图占位；不改变购物车、导航或数据。
- 本机验收：`tests/browser_navigation_thumbnails.cjs` 通过；两种语言、五种视口、长标题、缺图/损坏图片/空目录/接口失败、图片点击选择及 Escape 焦点恢复。所有 API 写请求禁止，未写真实记录。
- 实际目录只读检查：图片正常加载；工具首项 `BT-WB` 与附件首项 `TEST-ACC-04` 当前 `image_path` 为空，按规则显示占位，不跳用后续产品。截图在 `tmp/navigation-thumbnails/`（未纳入 Git）。
- Git：本增量未提交；远端：未部署 NAS/ECS。实际触屏真机仍待验收。

## 最新增量：UI 一致性与小屏适配

| 范围 | 已实现 | 本机验收 | Git | 远端部署 |
| --- | --- | --- | --- | --- |
| 弹窗裁切、报价列表、侧栏、个人中心入口与英文溢出 | 是；仅显示与布局修复 | [UI 专项记录](2026-09-12-ui-review.md)，隔离 fixture 布局回归；真机和全部真实业务状态仍待验收 | 基线 `e62f24e`，本轮未提交，未更改暂存区 | 本轮未发布 NAS/ECS |

设计规范已同步为 2026-09-12。未修改 API、数据库和 PDF 排版。以下表格及执行记录保留前次维护时的状态，不覆盖本节最新 UI 增量，也不表示重跑了全部后端测试。

## 前次维护状态总表（历史证据）

| 范围 | 已实现 | 本机验收 | Git | 远端部署 |
| --- | --- | --- | --- | --- |
| 目录、询价、分享、PDF 与界面累计功能 | 是；本轮修复询价转报价遗漏客户电话/地址 | 全量自动化 360 项通过；页面真实点击验收待补运行时 | 代码基线 `09f0131`；本轮改动未提交 | NAS/ECS 未部署本轮变更 |
| 数据库结构 | 是 | 本机迁移为 `20260911_0030`，完整性/外键检查已记录 | 迁移已在 `09f0131` | NAS 仍为历史发布基线 |
| 65 张被引用配置图片 | 是 | 文件解码、路径、LFS 属性、本地 LFS 实体与哈希通过 | 已暂存，未提交 | 未上传、未验证新克隆或 NAS |
| Logo 与 HarmonyOS Sans 字体 | 是 | 本机 HTTP 资源检查通过；字体许可文本已保留 | 已跟踪 | 未进行本轮镜像/NAS检查 |
| 发布预检与恢复校验 | 是；新增真实字体解析、路径大小写、全部历史上传图片解码及 Windows 恢复短锁重试 | `0030` 合成库恢复、损坏库拒绝及资源专项通过；最终全量回归执行中 | 本轮改动未提交 | 不执行远端发布 |

“已实现”不等于“已部署”；“本机验收”不等于真实客户数据验收。历史文档中早期“当前/未实施/未提交”表述只保留为当时证据，不覆盖本表。

## 本轮范围与环境

- 本机工作区：`E:\Project\CC-Project\benchshop`；不提交、不推送、不部署 NAS/ECS、不覆盖远端数据库。
- 真实业务只允许使用隔离数据库、临时上传目录、独立端口和测试账号；禁止使用客户账号或运行中的 NAS 数据卷。
- 已暂存的 65 张图片保持暂存状态。原始 Excel、其余 16 张配置图片和其他用户资料不加入本轮范围。

## 执行记录

| 项目 | 命令/环境 | 结果 |
| --- | --- | --- |
| 发布预检扩展 | 隔离 pytest fixture | `tests/test_release_bundle.py` 覆盖缺字体、损坏图片、源文件变化、篡改归档与恢复；包含在全量回归内 |
| 本机发布预检 | `release_bundle.py --check`，`backend/boten.db` | 迁移 `20260911_0030`、93 个图片引用及资源检查通过；唯一阻断为预期的未清洁 Git 工作区 |
| 全量 Python 回归（本轮最终） | 隔离临时数据库，`pytest -q --durations=20` | **360 通过、14 子测试通过**，耗时 10 分 08 秒；2 条第三方 Starlette TestClient 弃用警告，无测试失败或跳过 |
| 三角色真实 API 浏览器链路 | 浏览器 → `127.0.0.1:8092` → 隔离 API `127.0.0.1:8002` | 客户分享/导入、当前设备及购物车询价、业务员分配/转报价/发送、客户查看、分享复用/备注保留/关闭失效通过；未使用接口模拟 |
| 客户资料带入修复 | 隔离三角色询价转报价 | 修复了报价草稿漏带客户电话、地址；随后真实浏览器报价与 PDF 均确认保留两项信息 |
| 实际 PDF 浏览器下载与渲染 | 隔离 API + 浏览器 + Poppler | 分享、询价、报价中英文及个人中心报价共 7 次真实下载均可打开；文件名/页内编号、Logo、字体、页眉页脚、北京时间、长英文地址、分页、金额边界及空客户字段通过 |
| 发布包恢复演练 | 临时源库、临时归档与恢复目录 | 接收端先验证归档总哈希、成员集合及逐文件哈希；篡改包在写入恢复目录前被拒绝，验证后的数据库/图片恢复并通过完整性、外键、迁移、引用及哈希检查 |
| 资源预检补强 | `tests/test_release_bundle.py` | 15 项通过：非空损坏字体、历史上传图片、静态/CSS 路径大小写不一致、LFS 指针、篡改归档与源文件变化均会阻断；65 张 LFS 暂存图片未改动 |
| `0030` 隔离恢复演练 | `tests/test_release_recovery_0030.py` | 1 项通过：由真实迁移链创建合成设备、客户、配置、分享及上传图片，备份/原子恢复后迁移版本、外键、图片引用和哈希一致；损坏库在替换前被拒绝 |
| Windows SQLite 恢复短锁 | `backend/database_maintenance.py` | 新增有限退避重试，避免验证完成后短暂文件锁导致原子替换误失败；不改变恢复确认、备份或事务边界 |
| PDF 多页渲染复核 | Poppler 渲染 `tmp/pdfs/brand-layout` 六份中英文样稿 | 首页和续页共有 18 页可渲染；抽查配置清单中文与报价单英文首页，Logo、粗标题、英文长地址换行、文件编号/页码/导出时间均未重叠 |
| 本机资源预检 | `release_bundle.py --check --database backend/boten.db --uploads uploads/catalog` | 迁移 `20260911_0030`、93 个图片引用、字体和图片校验通过；唯一阻断仍是预期的未清洁 Git 工作区 |

## 剩余阻断项

- 推送获授权后，复核 65 张 LFS 图片在远程实体及全新克隆中的可用性；当前只验证了本地实体。
- 构建 Linux Web/API 镜像后，确认字体许可证、Logo、字体和图片仍实际存在；本轮未构建镜像。
- NAS/ECS 发布、远端备份/恢复和真实客户记录回归继续等待单独授权。
- 浏览器“真实点击”全流程尚未在本机重新运行：现有 `tests/browser_real_api_maintenance.cjs` 所依赖的 Node Playwright 包在当前运行时缺失，不能把 API 直连或历史下载记录冒充为页面点击验收。隔离 API/网页已在 `8002/8092` 就绪，待安装或指定可用浏览器自动化运行时后继续。
- 保留 2 条 Starlette TestClient 弃用警告，待依赖升级时处理；它们不影响本轮业务或发布结果。
- 其余 16 张未被当前数据库扫描引用的配置图片已记录为用途待确认，未确认前不删除或移动。

## 非本轮事项

安全深度审查、Excel/WPS 兼容、真实触屏、Linux 镜像扫描、NAS/ECS 发布及远端数据库兼容性合并均等待后续独立授权。NAS 产品/账号按本机新增或覆盖已确认对应记录，NAS 独有及业务历史保留；禁止全库替换，必须先生成双端差异和冲突清单。
