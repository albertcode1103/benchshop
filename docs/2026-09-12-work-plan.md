# 2026-09-12 今日合并工作计划

## 当前边界

- 本文件与 [本机维护记录](2026-09-12-maintenance-record.md) 共同构成当前状态入口：本文件记录待办与执行状态，维护记录保存已验收证据。
- 最新 UI 修复基线为 `e62f24e`；本轮不改数据库和图片。下方 `09f0131` 时期的维护勾选及图片暂存说明保留为历史记录，不作为本轮验收。
- 仅限本机和隔离测试环境；最新“更新 md，git”授权将本轮源码、测试和文档作本地提交。不得推送、部署 NAS/ECS、修改 NAS 数据库或真实客户记录。下方各条“未提交”为实施当时状态，本轮本地提交由维护记录最新检查点统一归档。

## 已迁入并执行中的任务

- [x] 购物车滚动列表全部文字统一 12px（设备/工具/附件信息、数量、列表按钮），页头、底部操作与详情弹窗不变。双语 393/320/1440px 计算字号、点击区与分类预览专项通过；同步旧契约的 14px 列表选择器及前次“查看目录”文案预期，未提交/部署。

- [x] 按后续要求，设备页按钮也统一为“目录 / Catalog”，与工具/附件一致；同步初始 HTML、双语文案、导航回退与缓存版本，功能不变。本机修改，未提交/部署。

- [x] 小屏已选配置抽屉底部保存/询价按钮固定，标题固定、中间配置与提示单独滚动，加入底部安全区和低高度适配。24 组双语短/长清单、手机/横屏/桌面、按钮点击区域及 Escape 焦点专项通过；58 项契约通过，未提交/部署。

- [x] 个人中心小屏顶栏改为 44px 圆形“中 / EN”切换按钮和房屋图标主页链接；桌面保留双语言和主页文字，补齐无障碍名称。132 组个人中心专项（含 430/768px 键盘切换语言、保留 Hash）及 58 项契约通过，已查看 320px 渲染；未提交/部署。

- [x] 工具/附件页目录按钮文案改为“目录 / Catalog”，初始 HTML、语言切换与导航初始化同步；设备页“查看目录 / View Catalog”不变，原目录功能不变。本机修改，未提交/部署。

- [x] 个人中心窄屏导航重排：账号摘要＋两组三列入口，取消横向长标签，保留主页、退出登录和未读徽章；同步表单/业务筛选/记录操作自适应，修复 320px 英文更多菜单越界。132 组双语/11 尺寸专项、58 项契约及 JS 语法通过；仅隔离前端验收，真机软键盘仍待验收，未提交/部署。

- [x] 从产品导航进入设备、维修工具或附件时，关闭目录并切换内容后回到页面最顶部（scrollY=0），不再仅对齐模块标题；仅取消/关闭目录不重置位置，配置内部标签的原定位不变。`browser_catalog_scroll.cjs` 覆盖中英文、430/1440px、减少动态效果、延迟目录加载、分类切换保留设备选项/购物车及取消位置；通过。58 项契约通过，未提交/部署。
- [x] 工具/附件页标题与“查看目录”按钮垂直居中，移除标题及按钮额外偏移，标题栏下间距缩为 12px。双语、430/320/1440px 对齐/间距/按钮尺寸检查及 58 项契约通过，原搜索与操作不变；未提交/部署。
- [x] 用户端工具/附件卡片字号按最新要求调整：编号 14px、名称 14px、描述/备注 10px（编号由前次 12px 改回 14px）。数量控件、按钮、双列及选中样式不变；前次中英文多视口与 58 项契约记录仅为当次证据。本次未提交/部署。
- [x] 用户端设备选配卡片编号/名称改为 14px，专有备注 12px；保留颜色、字重和双列布局。中英文、430/320/1440px 实际计算字号及键盘选择检查、58 项契约测试通过；仅局部 CSS，未提交/部署。
- [x] 移除用户端维修工具/设备附件标题下的说明段落及运行时填充，中英文一致；保留查看目录、搜索及原操作。430px 双语四入口隔离浏览器检查及 58 项前端契约测试通过；未提交/部署。
- [x] 工作台概览“近期分享”状态文字居中修复：限制 `.mini-row` 的说明文字样式，避免覆盖徽章 flex 布局和误加顶部间距。有效/已关闭四种视口专项通过，58 项前端契约测试通过；未改状态逻辑或颜色，未提交/部署。
- [x] 修复小屏二级导航焦点穿透与排序重绘焦点丢失，覆盖返回/关闭/断点切换、空列表、保存失败重试及异步完成不抢焦点；同步两项旧契约断言。58 项契约、540 项布局与导航/排序/设备可用性专项通过，证据见维护记录最新增量。仅本机，未提交或部署。
- [x] 浏览器标签页标题统一：客户端及个人中心 `BenchShop-博特恩检测设备`、工作台 `BenchShop-工作台`；中英文均保持此标题。

- [x] 产品导航左侧目录与右侧设备增加 1:1 缩略图；目录取当前语言/原排序首项图片，设备取自身首图，缺图占位。中英文、1428/640/390/320px 及 844×390 横屏、长标题、空目录、加载失败、选择和焦点恢复通过隔离专项；已只读检查本机真实目录。未提交、未部署，未改真实数据。
### CR918S 目录显示与设备编辑底栏（已实现，本机验收）

- [x] 修复已启用设备因配置分类为空被前台过滤的问题。CR918S 按中文可见、英文隐藏显示；缺少可用颜色或基本配置时仍可查看资料，并禁用保存/询价；不自动补写真实配置数据。
- [x] 语言切换/刷新仅恢复当前语言可见设备，否则选择该语言排序第一台，且不套用另一台设备的选项；无可见设备显示空状态，工具、附件、登录和购物车照常初始化，历史设备数据仍保留。
- [x] 底栏语言设置改为专用紧凑复选组，18px 橙色复选框、统一间距，取消突兀原生边框；触屏标签点击区至少 44px。
- [x] 底栏桌面设置靠左、操作靠右；小屏换行并保留按钮可达性，样式限定于设备发布设置，不影响其他弹窗。
- [x] 保留语言至少选一种和总启用字段、原保存参数/版本号；补充前端语言全未选提示。页头编辑语言与底栏目录可见语言仍独立。
- [x] `tests/browser_catalog_readiness.cjs` 隔离写请求回归：空配置/无颜色/空目录、语言回退/选项隔离、阻止提交与补齐恢复、异步保存期间切换、底栏三个尺寸与语言校验/保存参数通过；`tests/browser_device_visibility.cjs` 双语回归通过。真实触屏、软键盘和全业务状态仍待专项验收。

真实只读浏览器核验：CR918S 中文目录可见、英文目录隐藏。最初核对时没有基本配置；后续核验时真实接口已有电机 1 项、电源 2 项，因此当前保存/询价按钮按规则恢复可用。本轮未修改这些真实数据，也未提交 Git、部署 NAS。当前检查仅证明前端可用性与隔离提交参数，不代替真实业务价格/必选配置的服务端校验。

### 最新增量：UI 一致性与小屏适配

- [x] 设备目录语言范围移入状态徽章（如“已启用：中文 EN”“已启用：中文”“已下架：EN”），操作列仅保留编辑或数字排序控件。同步调整状态列宽度；双语、单语、下架及历史默认语言范围在 1440/320px 隔离浏览器回归通过，未改变启用或语言可见性数据。

- [x] 设备目录排序支持输入 1～设备总数的目标序号，点击“插入”或按 Enter 后稳定插入，其余设备顺移；保存前只改草稿，取消恢复原顺序。保留原批量排序 API、版本冲突检查，保存期间锁定排序操作。中英文 1440/320px 浏览器隔离回归及 `tests/test_product_navigation.py`（1 项）通过；未操作真实设备顺序，未提交或部署。

- [x] 修复短弹窗与登录注册裁切、报价列表零高及极窄行布局、侧栏滚动和个人中心显式入口。
- [x] 补修英文个人中心横向溢出和长文案确认窗裁切；保留原业务逻辑。
- [x] 更新设计规范、添加可重复的隔离浏览器布局回归，证据见 [UI 修复记录](2026-09-12-ui-review.md)。
- [ ] 真机 iOS/Android、软键盘、安全区、真实 200% 缩放及全业务状态验收；模拟视口不能替代。
- [ ] Git 提交与 NAS/ECS 发布，单独安排。本轮不更改远端数据。

### P0 资源与发布预检

- [x] 验证损坏字体、历史上传图片、路径大小写、LFS 实体和归档安全检查；65 张暂存图片与 16 张待确认原图均未改变状态。
- [x] 检查数据库的 93 个图片引用及发布静态路径；运行时动态路径仍需在 Linux 镜像专项复核。
- [x] 保留字体许可证，并让缺资源、损坏字体/图片、大小写错误和迁移不匹配成为可重复的阻断样例。

### P0 真实 `0030` 隔离恢复

- [x] 由迁移生成独立 `0030` 测试库并填充合成数据、上传图片；未复制真实客户数据。
- [x] 验证完整恢复、外键、迁移版本、图片引用及哈希；既有归档测试覆盖损坏包、成员缺失、路径穿越、重复成员和链接在写入前被拒绝。
- [x] 记录失败中止和回退前后哈希；该演练不代替 NAS 恢复验收。

### P0 三角色真实页面流程

- [ ] 使用隔离账号，通过页面点击完成客户分享/导入、设备与购物车询价、业务员分配、客户资料编辑、报价保存/发送及客户查看。**未完成：当前 Node Playwright 包缺失，隔离服务已准备但没有用 API 直连替代此验收。**
- [ ] 覆盖购物车、工作台和个人中心的语言选择、下载、取消、重试、有效分享配额与相同快照复用；确认备注保留、关闭失效及购物车状态保留。
- [ ] 覆盖桌面、手机视口、中英文、键盘和焦点恢复；自动化手机视口不替代真实触屏验收。

### P1 PDF 与遗留链路核对

- [x] 以隔离后端真实生成和 Poppler 渲染的样稿复核分享、询价、报价中英文的文件名、页内编号、Logo、字体、页眉页脚和金额边界；浏览器下载入口仍随页面点击验收保留未完成。
- [x] 补验长清单、多设备、缺失历史译文、空客户字段、扩位编号和跨页模块；分享/询价不得泄露报价金额。
- [x] 审核本轮涉及的请求模型、恢复接口、权限与并发保护；修复了恢复时 Windows 短暂文件锁。两条第三方 TestClient 弃用警告已单独记录，不在本轮顺带升级依赖。

## NAS 长期迁移注意事项

**最新确认：本机产品信息和用户账号新增或覆盖同一记录，NAS 独有记录保留，不直接替换全库。** 产品范围包括设备、配置、工具、附件、分类、价格、排序、映射及图片引用；已确认同一账号的资料、凭据哈希、角色和状态按本机覆盖。NAS 购物车、分享、询价、报价及历史快照不覆盖，不能顺带导入本机测试业务记录。每次同步先检查 NAS、双端备份并在隔离副本验证；身份/编号对应不明、跨目录或图片路径冲突须确认，保留旧业务引用。正式应用前复查 NAS 变化、幂等、事务、外键、图片完整性及失败回退。完整规则见 [项目执行规则](project-execution-rules.md#3-nas-数据迁移与兼容性合并)。本次仅更新文档，实际合并工具适配、演练与发布仍未执行。

## 待环境、授权或专项排期

- [ ] Linux 镜像内字体、Logo、静态资源和发布预检。
- [ ] 推送后的远程 LFS 实体与全新克隆恢复。
- [ ] NAS/ECS 发布及真实客户数据迁移；均待单独授权和维护窗口。
- [ ] 安全深度审查、Excel/WPS、真实触屏兼容。

## 收尾标准

- [x] 更新维护记录、README、执行规则和部署说明，分别标明已实现、本机验收、Git 与远端部署状态。
- [x] 已关闭本轮隔离服务；未向 8001/8081 或 NAS 数据卷发送停止命令，65 张图片仍保持暂存。
