# 2026-09-08 任务计划与技术路线

## 一、今日目标

1. 先收口 9 月 7 日已经完成但尚未人工验收、尚未提交 Git 的分享—询价—报价协作改造。
2. 补齐“分享导入购物车后再询价”的来源追踪，保证分享、询价、报价和发送记录可以完整反查。
3. 对失效目录项目、并发操作和异常恢复进行加固，不静默丢弃历史业务内容。
4. 完成桌面端、窄屏、手机端和 200% 缩放验收，并建立关键业务端到端回归基线。

## 二、执行边界

- 本轮只负责开发、调试和本机验收，最多同步到 Albert-NAS；不安排 ECS 或生产上线。
- 正式源码以 `E:\Project\CC-Project\benchshop` 为准，Albert-NAS 只作为联调副本。
- 自动化测试使用隔离数据库；涉及现有业务数据库前必须先备份，不运行会覆盖目录数据的初始化命令。
- 不删除历史分享、询价、报价、修订、发送记录和失效项目快照。
- 9 月 7 日工作区中已有改动先验收、再提交，避免和今天的新数据结构修改混成一个不可审查的提交。

## 三、昨日成果核对

### 已完成，不重复开发

- [x] 报价统一来源字段、来源索引和“每名业务员每个来源最多一份有效报价”约束。
- [x] 管理员和业务员可查看全部分享与询价；业务员报价列表只返回本人报价，管理员可查看全部报价。
- [x] 同一分享或询价允许不同业务员分别报价，本人重复转换复用原有效报价。
- [x] 分享、询价列表返回报价数量、报价业务员和本人报价 ID。
- [x] 本人报价一键打开；他人已报价时再次报价使用站内确认。
- [x] 后台分享 PDF、询价 PDF 不显示价格，正式报价 PDF 保留价格。
- [x] 报价编辑器支持设备普通配置增删、工具和附件增删、数量和实际单价修改。
- [x] 报价行按设备 → 工具 → 附件排序，并保留设备归属、序号、参考价和人工改价状态。
- [x] 服务端询价状态迁移规则、报价版本冲突和发送幂等已具备。
- [x] 分享码复制失败时已提供选中分享码和手动复制提示。
- [x] 报价筛选状态已进入 URL 状态，刷新和浏览器前进后退可恢复。
- [x] 上轮记录的完整自动化测试结果为 `94 passed`。

### 已完成代码、待用户验收

- [x] 分享列表与询价列表的报价状态、业务员姓名和更多菜单已通过桌面浏览器与响应式样式检查。
- [x] 本人报价可以一键打开；只有他人报价时，确认后能创建自己的报价。
- [x] 询价 PDF 内容、顺序正确，且不存在任何价格字段或价格标题。
- [x] 报价编辑器支持按设备增删普通配置，以及工具、附件增删与数量修改；自动化和浏览器联调均通过。
- [x] 已失效历史项目保留创建时快照并显示当前状态，保留或删除后均可正常保存。
- [x] 保存后重新打开、发送客户、报价 PDF、归档和恢复的内容一致。

## 四、P0：先恢复基线并完成昨日验收

### P0-1 本机服务与测试环境

- [x] 确认 API、静态站点和数据库路径，恢复本机用户端及后台验收入口。
- [x] 健康接口返回 HTTP 200；中英文产品接口均返回有效目录数据。
- [x] 数据库维护检查确认迁移、外键、索引和快照结构正常。
- [x] 恢复可运行完整测试的 Python 或容器环境，并用隔离数据库复跑全部测试。

技术路线：

1. 使用正式源码启动 API 和静态站点，不在 UNC 提供程序路径中拼接数据库路径。
2. 自动测试显式指定临时数据库；UI 联调使用现有验收数据库时先备份。
3. 同时检查健康接口、产品接口、登录接口和数据库维护结果，避免只验证静态页面。

### P0-2 四角色业务验收

- [x] 已准备与业务数据隔离的客户、业务员 A、业务员 B、管理员四种测试账号。
- [x] 已准备包含两台设备、维修工具和设备附件的隔离测试购物车，并完成混合内容验收。
- [x] 客户创建分享、当前设备询价和购物车询价均通过；幂等请求不会产生重复记录。
- [x] 两名业务员都能看到同一来源，并分别创建自己的报价，彼此不能查看对方报价详情。
- [x] 管理员能查看全部报价和业务员归属。
- [x] 报价保存、重新打开、发送、PDF、归档、恢复全链路一致。
- [x] 客户只能查看发送给自己的有效报价；归档后只保留历史摘要，不返回金额、明细或 PDF。

### P0-3 昨日版本收口

- [x] 修复验收中发现的权限、重复写入、价格泄露、快照丢失或 PDF 不一致问题。
- [x] 已在 9 月 7 日计划末尾增加 9 月 8 日收口记录，并更新最终测试结果。
- [x] 用户已于 2026-09-09 确认整理项目文档并执行 Git；提交不包含无关截图、临时 PDF 或渲染文件。

## 五、P1：补全来源、历史快照和异常恢复

### P1-1 分享 → 购物车 → 询价 → 报价来源追踪

实施状态：已完成。购物车来源轨迹字段、`customer_inquiry_sources` 多来源关联表、询价快照来源信息及后台聚合展示均已落地；关闭、过期和失效目录边界已覆盖自动化回归。

推荐数据结构：

- 为购物车中的设备配置、工具和附件保留可选来源分享 ID、来源项目 ID和来源文档版本。
- 新增 `customer_inquiry_sources` 关联表，允许一条询价关联零个、一个或多个分享记录。
- 在 `customer_inquiry_items.snapshot_json` 中固化来源分享码、来源项目和创建时可用状态。
- 报价继续使用现有统一来源字段；当直接由询价生成时以询价为直接来源，并通过询价来源表反查上游分享。
- 列表查询使用聚合查询返回来源数量和主要分享码，避免逐条查询。

验收：

- [x] 单一分享导入后询价可以反查原分享码，已通过接口和浏览器验证。
- [x] 两个分享混合进入购物车后，询价保留两个来源，已由隔离数据库自动化用例覆盖。
- [x] 分享内容与手动选择混合时，手动项目不被错误归到分享来源，已由自动化用例覆盖。
- [x] 原分享关闭、过期或目录引用失效后，历史询价和报价仍能根据不可变快照打开。

### P1-2 失效项目状态快照

- [x] 已定义 `active`、`inactive`、`missing`、`snapshot_only` 四种状态并写入询价快照。
- [x] 询价详情和询价转报价时重新计算当前状态，同时保留创建时状态，避免历史误判长期固化。
- [x] 报价重新打开时根据目录现状刷新展示状态，不覆盖历史名称、编号、数量和原始参考价。
- [x] 历史设备基础行不可删除；普通配置、工具和附件可由业务员明确移除。
- [x] 后台具备状态标签和中英文说明；产品停用、选项不可用、购物车项目不可用等错误均包含下一步操作提示。

技术路线：创建询价时通过目录引用检查器计算状态并写入快照；打开报价时只更新展示状态，不覆盖历史名称、编号、数量和原始参考价。

### P1-3 询价反馈与后台信息架构

- [x] 用户提交成功后显示询价编号、复制按钮和“查看我的询价”入口。
- [x] 询价详情已显示来源、关联分享码、负责人、报价业务员和基础处理时间线。
- [x] 已补充关联报价入口，并在个人中心增加可实际读取列表与详情的“我的询价”页面。
- [x] 详情时间线显示创建、接手、联系、首次报价、最近报价和关闭时间中已发生的节点。
- [x] 版本冲突后自动刷新询价列表，并提示“询价状态已更新，请重新操作”。
- [x] 转报价、接手和状态修改期间锁定当前按钮，其他行保持可用。

### P1-4 独立错误码与恢复路径

- [x] 分享码不存在、已过期、已关闭已实现独立错误码和中英文提示。
- [x] 不存在、已过期、已关闭三种分享状态已覆盖预览与导入接口自动化测试。
- [x] 产品不存在、产品停用、颜色不可用、配置不可用和购物车项目不可用均使用独立错误码与 HTTP 状态。
- [x] 中英文提示包含可执行的下一步操作，不出现 `[object Object]`、空提示或笼统“网络不可用”。
- [x] 询价、转报价和发送已具备幂等或并发防重复机制；仍需补做浏览器超时重试验收。

## 六、P2：界面、可访问性和代码收口

### P2-1 响应式验收

- [x] 已检查桌面宽屏、1024px、900px、720px、390px 响应式规则和 200% 缩放等效宽度。
- [x] 分享、询价、报价筛选栏使用统一控件高度和响应式网格，无固定空白列。
- [x] 表格更多菜单使用脱离表格裁剪的定位方式，并支持点击空白处或 Esc 关闭。
- [x] 报价编辑器移动端底部操作区改为两列换行，关键按钮保持至少 44px 点击高度。

### P2-2 键盘与焦点

- [x] 用户端与后台输入、按钮、选择框和菜单具备统一 `:focus-visible` 状态。
- [x] 关键动态弹窗与抽屉打开时移动焦点，关闭后返回触发按钮。
- [x] 原生模态弹窗负责焦点圈定；购物车抽屉与分享详情抽屉支持 Esc 和循环 Tab。
- [x] 装饰图标已从可访问名称中隐藏，图标按钮保留中文或英文 `aria-label`。

### P2-3 冗余清理与格式化

- [x] 已删除不再使用的购物车勾选、选中卡片和批量操作样式及对应旧语言键。
- [x] 日期统一使用 `Intl.DateTimeFormat`，数量、金额和币种统一使用 `Intl.NumberFormat`。
- [x] 设备型号、配置编号、分享码、询价编号和报价编号增加禁止浏览器误翻译标记。
- [x] README 已更新购物车、后台模块、测试说明、数据库路径和当前计划链接。

## 七、自动化与验收矩阵

### 后端自动化

- [x] 空数据库从最早版本升级到最新迁移。
- [x] 现有结构重复运行兼容迁移不报错、不丢数据。
- [x] 每名业务员每个来源最多一份有效报价；不同业务员可分别报价。
- [x] 询价、转报价、发送三条链路的幂等重试返回同一业务对象。
- [x] 客户、业务员、管理员权限边界正确。
- [x] 询价 PDF、分享 PDF 无价格；正式报价 PDF 有且仅有报价价格。
- [x] 设备、工具、附件混合排序与购物车规范一致。
- [x] 历史失效项目可读取、可保留、可删除，不破坏整份报价。

### 前端契约与端到端测试

- [x] 分享全部购物车并复制分享码已通过浏览器验收。
- [x] 当前设备询价和购物车询价成功反馈已通过浏览器与接口验收。
- [x] 两名业务员从同一来源分别报价。
- [x] 双窗口/旧版本保存触发版本冲突，后打开窗口不能覆盖新版本；自动化用例通过。
- [x] 报价发送、客户查看、后台归档、客户隐藏、后台恢复全流程。
- [x] PDF 下载文件名、语言、内容、排序和价格边界正确。

## 八、今日执行顺序与验收门槛

1. **第一批 P0：**恢复服务与测试环境，完成昨日六项人工验收并修复阻断问题。
2. **第一验收门槛：**核心链路通过、完整自动化测试通过、数据库检查通过；用户确认后提交昨日版本。
3. **第二批 P1：**实现多来源追踪、失效状态快照、询价信息架构和错误恢复。
4. **第二验收门槛：**单分享、多分享、混合手动内容以及失效来源全部可追溯。
5. **第三批 P2：**完成响应式、焦点、格式化、冗余清理和端到端测试。
6. **最终验收门槛：**桌面、手机、200% 缩放和四角色主流程全部通过，再更新文档与 Git。

## 九、今日实施技术路线（已完成）

### P0-A 失效状态双快照与报价重开一致性

目标：同时保存“创建询价时的状态”和“当前目录状态”，历史内容永不丢失，但后台能准确知道项目现在是否仍可用。

技术路线：

1. 在询价条目解码层保留 `captured_availability`，并通过统一目录检查器生成当前 `availability` 和 `availability_details`。
2. 设备基础配置同时检查旧版配置表和新版基本配置表，避免语言切换或目录重构后把正常电机、通道、电源误判为已删除。
3. 询价转报价时使用当前状态生成报价行；设备基础行、电源附加行仍锁定且保留快照，普通配置、工具和附件允许业务员明确删除。
4. 报价重新打开时由后端返回目录状态映射；活动目录仍用于“新增项目”选择器，停用或缺失项目只作为历史状态返回，不重新进入可选列表。
5. 增加“创建时正常→后来停用”“创建时正常→后来删除”“旧快照无状态字段”三类回归用例。

通过条件：正常项目不出现误报；失效项目有明确标签；任何状态下询价和报价都能打开，且历史名称、数量与价格快照不被覆盖。

### P0-B 混合购物车与多来源端到端验收

技术路线：

1. 使用隔离验收数据库建立两份分享：一份含设备、工具和附件，一份只含工具或附件。
2. 将两份分享导入同一客户购物车，再加入一项手动内容，验证数量合并时来源份额不会被覆盖。
3. 分别提交当前设备询价和整车询价，验证幂等键重放只返回原询价。
4. 在后台检查来源分享码、手动项目数量、设备→工具→附件排序和询价 PDF 无价格边界。
5. 两名业务员分别转报价，验证本人复用、他人隔离、管理员汇总、发送、归档和恢复。

通过条件：单分享、多分享、手动混合、无设备内容均能完成“分享→购物车→询价→报价→发送”，且来源可反查、数量不丢失。

### P0-C 独立错误码和异常恢复

技术路线：

1. 为分享码不存在、已关闭、已过期补齐预览与导入接口测试，分别验证 HTTP 状态、错误码和中英文文案。
2. 将产品停用、目录项停用、目录项删除、数量非法、快照损坏分开映射，提示中必须给出“重新选择、保留历史或联系客户”等下一步操作。
3. 后台收到 `INQUIRY_STATUS_CONFLICT` 时仅刷新对应询价和当前筛选结果，不清空列表，也不关闭用户正在查看的其他内容。
4. 通过重复请求验证询价、转报价和发送不会产生重复记录。

通过条件：不出现 `[object Object]`、空白提示或把业务错误统一显示成“网络暂不可用”。

### P1-A 客户询价反馈与个人中心

技术路线：

1. 询价成功弹窗保留编号，增加复制按钮、关闭按钮和“查看我的询价”按钮。
2. 个人中心新增“我的询价”一级页面，调用现有 `/api/v1/customer/me/inquiries` 与详情接口，支持状态、来源摘要、创建时间和取消操作。
3. 成功提交后不立即清空当前配置或购物车，用户主动离开时再按既有逻辑处理，避免误操作造成内容丢失。

通过条件：用户能从成功提示直接进入对应询价；刷新个人中心仍能找到记录；重复点击只显示同一编号。

### P1-B 后台询价时间线和协作状态

技术路线：

1. 增加并维护首次接手时间 `assigned_at`，状态首次进入 `contacted` 时写入 `contacted_at`，报价继续维护首次与最近报价时间。
2. 详情时间线按创建→接手→联系→首次报价→最近报价→关闭排序，只展示存在的节点。
3. 关联报价按当前账号权限提供入口：本人可直接打开，管理员可打开全部，业务员不得读取他人报价详情。
4. 列表继续使用聚合查询返回来源和报价摘要，避免逐行请求。

通过条件：负责人、报价业务员和时间线含义不混淆；权限按钮与后端权限一致。

### P2 UI、可访问性与冗余清理

技术路线：

1. 依次验收 1440px、1024px、900px、720px、390px 和 200% 缩放，重点检查筛选栏、更多菜单、详情抽屉和报价编辑器。
2. 更多菜单使用脱离滚动裁剪的浮层并支持点击空白处关闭；弹窗实现焦点进入、焦点圈定、Esc 关闭和焦点返回。
3. 删除无引用的购物车勾选与批量操作样式，但保留工具、附件选项卡的已选中边框和勾选标记。
4. 日期、数量、金额分别收口到 `Intl.DateTimeFormat` 与 `Intl.NumberFormat`；业务编号增加禁止误翻译标记。
5. 更新 README、9 月 7 日计划最终状态和本文件验收结果。

通过条件：桌面、窄屏、手机和 200% 缩放均能完成主流程；键盘可操作；无样式残留和过期文档描述。

### 今日验收门槛

1. 定向测试覆盖来源追踪、失效状态、分享错误码、询价冲突和客户询价页面。
2. 完整自动化测试在隔离数据库通过，数据库维护检查通过，前端 JavaScript 语法和契约测试通过。
3. 浏览器完成客户、业务员 A、业务员 B、管理员四角色验收。
4. 用户确认后再整理 Git 提交；不包含截图、临时 PDF、隔离数据库或其他验收产物。

## 十、开始执行记录

- [x] 已对照 9 月 7 日计划、当前代码和测试记录，区分真实缺口与过期未勾选项。
- [x] 已确认当前正式工作区包含 9 月 7 日尚未提交的功能改动及迁移文件。
- [x] 已确认本机 `127.0.0.1:8081` 当前未运行，Windows 当前命令环境没有可直接运行测试的 Python 解释器。
- [x] 已找到并恢复项目虚拟环境，完整自动化测试重新验证为 `94 passed`；数据库维护检查和代码差异格式检查通过。
- [x] 已恢复本机 `8001/8081` 验收入口，中英文产品接口均返回 7 台设备。
- [x] 已创建与业务数据隔离的验收数据库和客户、两名业务员、管理员四类测试账号。
- [x] 客户端已验证登录、保存设备配置、生成分享码、复制分享码和提交购物车询价。
- [x] 后台业务员已看到新询价，并能将其转换为自己的报价草稿。
- [x] P0 验收发现并修复：设备基础价格按所选电机/通道恢复，电机、电源和设备基础行不可删除，客户姓名、邮箱和接收账号可稳定回填。
- [x] 已通过浏览器验证第二名业务员看到首名业务员姓名、重复报价确认、独立建单以及本人报价快速打开。
- [x] 已通过接口验证业务员之间报价详情隔离（返回 404），管理员可查看两名业务员的全部报价。
- [x] 已实际生成并渲染询价 PDF 与正式报价 PDF：询价文件无价格，正式报价保留 CNY 单价和合计，中文与页码正常。
- [x] 已完成报价发送、客户读取、归档隐藏金额与明细、恢复后重新显示的状态闭环。
- [x] 本轮修复后完整自动化结果为 `94 passed, 1 warning`；唯一警告为 Python 3.8 依赖弃用提示，不影响当前功能。
- [x] 已完成包含两台设备、工具和附件的混合购物车验收，以及当前设备询价与购物车询价并列验收。
- [x] 已实现分享导入购物车后的来源轨迹、多来源询价关联表、来源聚合查询与后台来源展示。
- [x] 已通过自动化用例验证两个分享与手动项目混合时，询价保留正确来源；相关定向测试最近结果为 `53 passed, 1 warning`。
- [x] 已实现分享码不存在、已过期、已关闭的独立错误码及中英文提示，并补齐三类显式接口回归。
- [x] 已完成“创建时状态 + 当前状态”双快照收口，并重启本机 API 验证正常配置不会被误报为失效。

## 十一、最终执行结果（2026-09-08）

### 数据链路

- [x] 购物车项目可同时记录多个来源分享份额；手动添加数量保持独立，不会被错误归到某个分享码。
- [x] 询价保留上游分享多对多关系，后台列表与详情使用聚合查询返回来源，不产生逐行查询。
- [x] 询价项目同时保存创建时可用状态与当前目录状态；目录停用、删除或分享关闭不影响历史询价和报价读取。
- [x] 询价转报价、报价重开、修改、发送、归档和恢复均保留来源快照、设备归属、数量和历史价格。
- [x] 新报价创建时即分配 `BTQ-YYYYMMDD-####` 编号；旧草稿在下次保存时补分配，发送逻辑保留兼容兜底。

### 权限与协作

- [x] 所有管理员和业务员可查看全部分享、询价；业务员只能读取自己的报价，管理员可读取全部报价。
- [x] 两名业务员可针对同一来源分别报价；本人重复转换复用原有效报价，数据库约束与幂等键共同防止重复。
- [x] 询价负责人、首次接手时间、首次联系时间、首次报价时间和最近报价时间含义相互独立。
- [x] 个人中心“我的询价”可查看本人列表、详情、来源和状态，并按规则取消尚未处理的询价。

### 错误与恢复

- [x] 分享不存在、关闭、过期以及产品不存在、产品停用、颜色不可用、配置不可用、购物车项目不可用均提供独立错误码。
- [x] 中英文错误文案给出重新选择、刷新购物车或联系销售等可执行建议。
- [x] 询价版本冲突会刷新列表并提示重新操作；当前筛选与其他记录不会被清空。

### 验证结果

- [x] 完整自动化测试：`96 passed, 1 warning`，唯一警告为 Python 3.8 依赖弃用提示。
- [x] 后端 Python 编译、用户端与后台 JavaScript 语法、前端契约及代码差异格式检查通过。
- [x] 隔离验收数据库维护检查通过；本机 API 与静态站点保持运行，可直接进行用户验收。
- [x] 响应式截图覆盖 1440px、1024px、900px、720px 与窄屏；720px 可作为 1440px 浏览器 200% 缩放的等效 CSS 宽度。
- [x] 真实 390px 浏览器复验发现并修复报价项目列表被挤压及横向溢出：移动端外层可滚动、项目区保留 320px、高频操作区吸附底部且按钮不低于 44px。

### 当前边界

- [x] 用户已确认执行文档收口与 Git 提交；提交前完整自动化测试为 `98 passed`，代码差异格式检查通过。
- [x] 本轮未执行 ECS、生产环境或 NAS 部署；如后续明确要求，只允许同步调试到 Albert-NAS。

## 十二、PDF 统一改造收口（2026-09-09）

- [x] 当前配置、已保存配置、批量配置、购物车、分享记录、询价单和报价单切换到统一 PDF 生成器，原接口地址和权限保持不变。
- [x] 每页统一显示 BOTEN Logo、公司名称、品牌分隔线、文件编码、页码、生成时间和网站地址。
- [x] 配置清单、询价单和报价单使用固定主标题；报价草稿名称作为“报价主题”信息，不替代主标题。
- [x] 客户信息、报价人信息、询价备注、状态、币种、创建时间和有效期按文档类型组合显示，空客户字段不使用占位符。
- [x] 服务端按项目聚合规范重新整理设备、设备配置、维修工具、设备附件和其他历史项目，不依赖前端传入顺序。
- [x] 配置清单和询价单不包含任何价格列或金额；报价单显示单价、小计、分组小计和总计。
- [x] 报价详情响应补充报价人姓名、邮箱和电话；新增字段保持向后兼容，未修改既有权限模型。
- [x] 生成配置清单、询价单、中文报价单和英文报价单四类压力样稿，并使用 Poppler 逐页检查 Logo、字体、换行、分页、金额对齐和页脚。
- [x] 完整自动化测试结果为 `98 passed`；迁移头为 `20260908_0024`；`git diff --check` 通过。
- [x] 本次仅完成开发、调试、文档和 Git 收口，未部署到 Albert-NAS、ECS 或生产环境。
