# 2026-09-11 界面与数据链路验收

范围：本机个人中心、报价客户编辑弹窗及业务 PDF；依据原项目 UI 规范及用户指定的 web-design-guidelines。不是全站无障碍认证，也不代表 NAS 已发布。

## 已处理的审查项

- `account/account.css:34` — 桌面表单标签与输入同排，手机转单列，保留现有颜色、字体、圆角与间距变量。
- `account/account.css:151` — 共享样式曾让个人中心手机语言切换变成无文字圆点；恢复两个可见语言按钮，不改变主页的独立切换机制。
- `account/account.js:146` — 记录未保存表单状态，单独保存地址不重置其他未保存输入，离开页面有提示。
- `admin/admin.js:1668` — 客户弹窗使用带标签的四字段、正确输入类型与 autocomplete，异步搜索提示、取消不应用、关闭返回焦点；手机不自动聚焦弹出键盘。输入上限与报价 API 对齐。
- `backend/personal_business.py:18` — 删除动作改为用户隔离的可恢复隐藏，服务端核验所有权；不删除业务快照，分享关闭与隐藏分离。
- `backend/quote_categories.py:39` — 兼容旧直接快照及中英文封装快照，仅读取来源元数据补齐草稿分类；已发送报价不改写。
- `backend/pdf_service.py:758` — 固定字段标签以文档语言为准，不再由币种决定外观标题语言。

## 验证证据与边界

- `tests/browser_personal_quote.cjs`：1440px 桌面和 390px 手机，截图与布局溢出检查；地址局部保存、隐藏/恢复、客户选择、取消、应用、手动解绑与发送按钮限制。拦截全部 API，未写入真实账号或报价。
- `tests/test_quote_categories.py`：相同分类名称但不同 ID 不合并、同型号设备不合并、价格保持、不可变双语来源读取及已发送快照保护、基础供电排序、英文字段标签与统一加粗标题。
- 全量 315 项、14 个子测试通过；最终增补及前端/PDF 专项 67 项通过。两条测试依赖弃用警告不影响结果。
- PDF 样稿保存在忽略目录 `tmp/pdfs/current-layout/`，中英文询价各 7 页，报价中文 7 页、英文 8 页；逐页检查 Logo、表头重复、编号独立列、分类顺序及标题。末页剩余内容自然排版，不挤压字体填满页面。
- 本机数据库升级至 0027，有升级前备份，关键表数量保持，完整性及外键检查通过；真实报价只读加载成功。

## 仍待验收

- 用户真实登录状态下三角色的保存、发送、取消和历史记录流程；不以测试夹具代替。
- 旧发送快照若没有分类元数据仍保留原貌，不用当前目录强行重写历史。
- 200% 浏览器缩放、屏幕阅读器和完整键盘导航的全站专项检查。
- NAS 镜像构建、数据库覆盖、资源同步及 PDF Logo 验证；本轮未执行 Git 提交、推送或 NAS 部署。
