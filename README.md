# BOTEN 柴油试验台在线配置与报价系统

项目当前状态、每日进展与有效待办统一见 [项目更新日志](docs/CHANGELOG.md)。后续工作按日志维护约定更新，同一天补充同一日期节。

2026-09-13 仪表盘扩展：管理员与业务员默认首页增加待办、7/30天业务趋势、近期业务、目录汇总及管理员操作动态，使用独立只读统计接口。仅本机实现与隔离验证，未发布NAS；8082连接旧API时会提示先升级兼容接口。详见 [仪表盘实施记录](docs/2026-09-13-dashboard-implementation.md)。

这是一个面向客户、业务员和管理员的全栈选配系统。客户可以在线选择设备及工装、保存方案并生成分享码；业务员可以查看客户方案、制作双币种报价并下载 PDF；管理员负责设备、配置、账号和全部报价数据。

## 当前成果

本轮工作台调整、最终规则、验证证据与发布边界集中见 [2026-09-17 更新汇总](docs/2026-09-17-workbench-update.md)。包含统一信息备注、价格问号、询价筛选对齐及业务进度分行；均为本机实现，NAS 发布需单独核对。

### 2026-09-17 首页与设备页展示调整（本机）

首页公司全称独立置顶，与右侧画布顶部对齐，中英文均为 24px；设备型号与名称分两行显示为 16px，轮播改为平滑横向滑动。设备页按标注统一局部 14px/16px 字号并收紧参数表行距。本批为前端修改，由用户单独部署，最终规则与验证边界见[更新记录](docs/2026-09-17-home-device-ui.md)。

### 2026-09-17 报价历史快照、预览与应用（本机）

新版报价每次保存留存快照，历史界面区分记录版本 V 与快照 R，支持具体明细预览及载入编辑器后保存。保留并发校验、归属权限和已发送内容不可变规则；升级前未留存的编辑无法追溯。需先发布 API 再发布前端，无新增表，见[实施记录](docs/2026-09-17-quote-history.md)。

### 2026-09-17 用户端目录备注隔离（本机）

配置、工具、附件卡片仅显示编号、名称、描述，备注及设备专属备注保留在工作台。同步移除备注搜索、描述兜底及购物车展示，用户接口过滤历史快照中的目录备注。需更新 API 与前端，无数据库迁移，详见[实施记录](docs/2026-09-17-customer-catalog-notes.md)。

### 2026-09-17 设备参数表新增行保留输入（本机）

修复设备编辑时点击“添加参数”可能覆盖未保存输入的问题：新增前先采集当前参数，并保留另一语言内容。连续添加、中英文切换、排序/删除、保存及重开专项通过，58项契约通过。未部署NAS、未修改正式数据，见[修复记录](docs/2026-09-17-product-specifications-fix.md)。

### 2026-09-14 分享备注与 PDF 时间页脚（本机，待用户部署）

我的分享卡片新增“配置备注 / Note”及更多菜单导出 PDF；分享配置清单在分享人右侧显示配置备注。前后台分享配置清单、询价单、报价单统一移除标题下时间，每页右下角分两行显示单据创建时间与 PDF 导出时间。包含兼容的列表 `note` 响应扩展，无新增迁移；需更新 Web 与 API。实施、验证及部署后检查见 [本批更新记录](docs/2026-09-14-share-pdf-update.md)。

### 2026-09-13 用户端选配与购物车细化（本机）

统一选配区与购物车字号、卡片和摘要布局；空配置保存/询价增加定位与确认，同型号保存增加覆盖/新增选择，购物车新增确认清空并修复空态居中。完善分享预览、个人中心记录操作、询价来源和中英文显示。本批复用现有 NAS API，无新增数据库结构；已纳入本次 Git 版本，尚未部署 NAS。具体规则、隔离测试证据和待验收项见 [选配与购物车更新记录](docs/2026-09-13-customer-cart-implementation.md)。

### 2026-09-13 报价分类标题与滚动固定栏（本机）

报价编辑器新增每台设备的基础配置及可选分类分隔标题；固定栏随正反向滚动显示“设备序号 · 型号 · 当前分类”，工具/附件显示对应组名。支持手机和低高度窗口，不改变报价项目顺序、数值或保存流程。分类滚动专项、58项契约、540项布局检查通过。本项已纳入本次 Git 版本，尚未发布 NAS；详见[报价分类实施记录](docs/2026-09-13-quote-sections-implementation.md)。


### 2026-09-13 产品入口主页（本机）

用户端新增主页，产品导航最上方提供“主页 / Home”；普通访问与 Logo 返回主页，刷新/语言切换保持当前视图。右侧按目录顺序每 5 秒轮播当前语言可见设备，显示图片、型号和标题；左右箭头位于图片两侧中央，不显示顶部“设备目录、页码、暂停”整行。保留工具、附件入口及选配、购物车和原业务流程，适配手机与深浅色。悬停、焦点、触摸、隐藏及弹层打开时暂停，减少动态效果下仅手动切换。

本轮主页修改已纳入本次 Git 版本，尚未部署 NAS、未修改正式数据。最终显示版本已通过 3 组自动轮播专项及 4 组主页流程补测；早期 32 组主页检查等历史证据与真机待验收项分开记录，见 [主页实施记录](docs/homepage-implementation-20260913.md)。

### 2026-09-13 用户端深色模式（本机已实现）

首页账号菜单“退出登录”上方可切换深色模式；初次跟随系统、手动偏好按浏览器保存，首页/个人中心保持一致。仅显示层更新，不新增 API、不迁移数据库，不影响工作台或 PDF。中英文及桌面/手机主题专项、61 项前端契约/本地代理测试通过；主题截图已检查，真机及全业务状态待验收。已纳入本次 Git 版本，尚未部署 NAS。

### 2026-09-13 自动滚动修复（本机）

颜色/选项修改不再回顶或收起概况，分类标签仅横向定位，抽屉关闭恢复焦点不滚动背景；购物车编辑/快捷定位统一避让页眉和减少动态效果。历史返回恢复位置，同设备切换语言恢复浏览区域，主动目录导航仍回顶。规则见设计规范。本轮仅本机前端更新，未修改 NAS 数据或后端；已纳入本次 Git 版本，尚未部署。

验证：`tests/browser_scroll_policy.cjs` 在 1280/393px 下通过颜色、概况展开、键盘页签、历史返回、语言锚点及目录回顶检查（NAS 请求仅 GET/HEAD，写入拦截）；前端契约 58 项通过。浏览器测试不能替代真实手机软键盘及全部业务状态验收。

### 本机启动入口（2026-09-13 已切换）

默认只启动前端代理：`.\backend\.venv312\Scripts\python.exe -m deploy.local_dev_server`，访问 `http://127.0.0.1:8082/`。API（含上传/PDF）转发至 `http://192.168.31.69:8080`，不启动本机后端、不访问本机数据库；正式数据写入受 NAS 账号权限约束，页面显示 NAS 可写提示，断连返回 502、不重试或回退。

NAS 模式使用新的 8082 origin，避免读取原 8081 登录和草稿；需要重新登录 NAS 账号。默认仅本机访问；手机调试需显式增加 `--bind 0.0.0.0` 并仅对可信局域网开放防火墙。只读验证不代替真实登录、上传和 PDF 验收。

独立后端测试必须明确使用 `--mode isolated --port 8081`（或 `--api-port <隔离API端口>`），并另行启动指定隔离数据库的 API；不得对 NAS 正式数据执行写入型自动化回归。下方历史“双窗口”命令仅适用于此隔离模式，网站启动命令须加 `--mode isolated`。

### 2026-09-13 正式数据与发布原则（当前规则）

NAS 已正式使用，停止日常从本机覆盖产品、用户及业务数据。默认开发架构确定为本机前端通过 NAS API 读写正式数据，不直接访问 SQLite；代理模式和正式环境提示尚待实施，当前文档更新不代表已切换运行环境。

每次发布必须检查 API/数据库差异；有变更先说明兼容性、迁移版本、备份、验证及回退，并取得明确确认，再执行后端启动/升级。普通 UI 发布只更新前端；后端调试用隔离数据库副本。详见 [执行规则](docs/project-execution-rules.md#0-正式数据与发布强制原则2026-09-13) 与 [部署门禁](deploy/README.md)。这是强制发布检查约定，自动通知/检测尚未实现。

NAS 发布还必须先按文件清单、哈希及镜像影响区分快速部署与全量覆盖。小型前端修改默认复用 Docker 缓存并只重建 `web`；`--no-cache` 仅用于依赖、镜像定义或构建规则变化、从零构建验证，以及已有证据表明缓存未纳入候选文件的情况，并应限制到受影响服务。

下方日期段落为历史记录；其中“未部署”及“本机数据覆盖 NAS”不代表当前状态或授权。`0d8bcc7` 已同步 NAS，9 月 12 日专项合并由用户执行并报告 `APPLIED 22 rows`，随后服务健康且新版页面资源只读检查通过；不代表全部真实业务验收完成。

### 2026-09-12 本地 Git 检查点（当前状态）

本节随本轮源码、测试与文档一起保存为本地提交，提交前基线 `e62f24e`。下方各增量的“未提交”是实施时历史记录，以本节及维护记录最新检查点为准。**未推送远端、未部署 NAS/ECS、未同步数据库。**

- 汇总个人中心窄屏导航与圆形顶栏、弹窗/报价编辑适配、已选配置固定底部操作、购物车列表 12px，以及目录缩略图、设备数字插入排序/显示范围、导航回顶部与“目录 / Catalog”文案。
- 设计规范、今日计划、UI 审核记录及 NAS 兼容合并规则同步纳入；保留 NAS 独有记录的规则不代表已实现或执行合并工具。
- 验收范围与命令见 [本机维护记录](docs/2026-09-12-maintenance-record.md)。浏览器使用隔离接口，不冒充真实客户流程或真机验收。Excel、PSD、原图、数据库和临时测试产物不纳入本次提交。

个人中心小屏导航已改为账号摘要及两组三列入口，六个入口无需横向滑动；同步表单、业务筛选和操作菜单的自适应布局，保留桌面侧栏与原业务流程。132 组双语/11 尺寸前端专项及 58 项契约通过；真机软键盘待验收，未提交 Git 或部署 NAS。详见 [UI 修复记录](docs/2026-09-12-ui-review.md)。

本机故障修复增量：小屏设备导航与排序模式的键盘焦点已修复，旧契约测试已同步当前规则；58 项契约、540 项布局及导航/排序/设备可用性专项通过。标题统一为客户端 `BenchShop-博特恩检测设备`、工作台 `BenchShop-工作台`。未提交 Git 或部署 NAS，真机与完整业务回归仍待验收。

产品导航现支持文字左侧 1:1 缩略图：目录自动取当前语言首项图片，设备展示自身首图，缺图显示占位。两种语言和五种视口的隔离浏览器专项通过，本机真实目录已只读检查；本增量未提交或部署。

> 当前代码基线为 `e62f24e`；本轮 UI 显示与小屏适配修复尚未提交或发布，详见 [UI 修复记录](docs/2026-09-12-ui-review.md) 和 [设计规范](docs/website-design-system.md)。维护入口仍为 [本机维护记录](docs/2026-09-12-maintenance-record.md) 与 [今日合并工作计划](docs/2026-09-12-work-plan.md)。360 项测试、14 项子测试及 65 张图片暂存的旧描述属于前次维护证据，不代表本轮验证或当前暂存状态。NAS 最新同步规则：本机产品信息与用户账号新增或覆盖已确认对应记录，NAS 独有记录保留，分享/询价/报价等业务历史不覆盖；先备份并在隔离副本验证兼容性合并，禁止直接替换全库。详见 [项目执行规则](docs/project-execution-rules.md#3-nas-数据迁移与兼容性合并)。本次仅更新规则，未实际同步。

本机增量：设备目录保留尚未配置完成的已启用设备，按当前语言可见性显示；缺少可用颜色/基本配置时提示并限制保存、询价。切换语言会跳过隐藏设备，保留购物车和历史记录。设备编辑底栏的语言复选框已紧凑化；详见今日计划的 CR918S 验收记录。本增量未提交或部署。

### 2026-09-11 本地 Git 整理检查点

用户已授权将累计本机代码、迁移、测试和文档整理为本地提交；不推送、不部署 NAS、不覆盖远端数据库。下文“未提交”为历史检查点，以本节和任务计划顶部为准。原始 `tb` 表格、图片不纳入本次提交，发布图片核验与跟踪仍待完成。

- 修复设备询价多传 `name` 导致 422，错误不再显示 `[object Object]`；模拟接口验证字段、失败提示与重试成功，未创建真实询价。
- 选配分类按钮显示实时已选数量：0 件深灰底白字，大于 0 为购物车同款橙色气泡，已验证选择与取消。
- 工具/附件分类列表统一边距：桌面左右 24px，分类间距 16px，手机左右 16px；独立布局测试覆盖 1428px/390px。
- 本次 JavaScript 语法检查及 58 项前端契约测试通过；此前专项验收记录保留。全量回归、真实业务流程及 NAS 验收仍未完成。

购物车PDF导出支持复用本用户内容完全一致的有效分享：服务端核对双语快照、数量和顺序，匹配后保留原分享码、备注及有效期，不增加名额；关闭/过期和其他用户的分享不参与匹配。仅PDF流程启用，不改变独立分享入口行为。

PDF模板最新本机更新：所有文件页眉统一Logo及公司名称/地址/网址，页脚统一文件编号/页码/北京时间；主标题加粗，英文报价标题为Price List，分享信息标题为“分享人 / Shared by”。询价/报价业务时间移到首页标题下方。23项PDF兼容测试、9项排版专项及18页样稿视觉检查通过；详情见任务计划顶部。没有Git提交或NAS部署。

最新实施轮：设备语言范围/排序、业务进度合并、购物车分享后PDF及统一文件/页内编号已接入，本机已备份迁移至0030并重启API。隔离专项、浏览器与六份PDF样稿检查已有通过证据，最终全量回归仍在进行；详见任务计划顶部。以下旧“未实施”说明作为历史检查点保留，不代表最新代码。未提交Git、未部署NAS。

本轮范围包括个人中心分享详情/文案、购物车分类预览、工具/附件分类管理、设备语言与排序、业务进度合并、分享后导出和 PDF 编号统一。以 [任务进度汇总](docs/2026-09-11-task-plan.md) 顶部为当前状态入口，以下日期记录保留历史上下文。

2026-09-11 查漏修复：服务端统一禁止目录项目跨配置/工具/附件改类，同目录调整不受影响。新增私有发布包预检工具 `deploy/release_bundle.py`（工作区、数据库迁移、图片/LFS 实体、快照与归档哈希检查），不会自动传输或覆盖 NAS。当前 65 张已引用静态图尚未跟踪，发布预检会阻断；用户最新要求暂不提交、不部署。当前状态统一看 [计划顶部查漏清单](docs/2026-09-11-task-plan.md)。

2026-09-11 本轮本机更新（未提交、未部署）：工作台 PDF 导出前选择中英文；普通用户有效分享上限 10 份，删除同时关闭分享码并释放名额；工作台分享默认筛选有效记录。工作台入口改为新标签页，工具/附件补齐查看目录，修复目录关闭按钮，购物车导出与分享移至底部，两处报价按钮统一蓝色。工具四类与附件六类已实施必选分类和前后台筛选；0028 迁移已备份后应用本机数据库，仅修改目录分类及版本，不改写历史业务快照，详见 [分类映射与执行记录](docs/2026-09-11-category-mapping-preview.md)。实际验收及剩余项见 [今日计划](docs/2026-09-11-task-plan.md)。

2026-09-11 本机新增：个人业务记录搜索/筛选及可恢复隐藏，分享关闭/开放，联系方式中的独立地址保存；报价客户选择和四字段编辑合并为弹窗。报价配置携带分类与排序元数据，PDF 一级分组标题统一。新增数据库迁移 `20260911_0027`，正常 API 启动自动备份并升级。实现与测试状态见下方工作计划；不代表 NAS 已更新。

截至 2026-09-11，项目已经具备可本地运行和联调的主流程；本机实现不等于 NAS 部署已完成。最新进展和未完成项见 [工作计划](docs/2026-09-11-task-plan.md)。

- 用户端支持 7 个设备型号，按机型独立映射颜色、电机、供电和可选工装。
- 配置目录包含 CRI、HEUI、EUI/EUP、CRP、Cambox Extension 等分类；当前示例数据库有 8 个分类、75 个配置项和 175 条机型映射。
- 单个机型可以为共享配置分别填写中文和英文专属说明，避免提示影响其他设备。
- 中英文切换会保留当前设备和已选配置；设备、分类、配置名称及描述从后台数据库读取。
- 手机端提供紧凑导航、配置抽屉、关闭按钮及悬浮查看按钮。
- 注册要求姓名、邮箱、国际格式手机号和密码；登录可选择邮箱或手机号，游客可浏览，保存和分享时必须登录。
- 登录用户可保存多套设备配置，也可独立加入维修工具和设备附件；购物车按“设备 → 工具 → 附件”聚合，顶部操作默认分享或导出全部内容，设备、工具和附件卡片支持分别编辑与确认删除，分享内容使用不可变双语快照并保留 90 天。
- 管理后台包含设备目录、配置目录、工具目录、附件目录、分享记录、询价列表、报价管理和账号管理。
- 设备与配置可分别维护中文/英文内容、人民币/美元参考价、图片和启用状态；配置与颜色图片可在后台直接上传。
- 当前配置、已保存配置、批量配置、购物车、分享记录、询价单和报价单均由后端统一生成跨平台 PDF，不再依赖浏览器打印或 Windows Edge。PDF 统一使用 BOTEN 页眉、客户/报价人信息卡片、业务聚合表格和带总页数的页脚。
- 业务员可查看分享用户的联系方式和详细配置，按 CNY/USD 自动填价或手动调价，保存并下载报价 PDF。
- 管理员可查看全部报价和账号，业务员只可管理自己的报价。
- 管理后台新增操作审计，记录管理员和业务员成功的写入操作，但不记录密码和表单正文。
- 已提供数据库在线备份、完整性检查、确认式恢复和目录数据质量审计命令。
- 自动化测试覆盖迁移、认证、账号生命周期、价格边界、多设备分享、三角色询价报价发送、历史快照、PDF、Excel 解析资源限制、版本冲突及静态文件保护；最新实测数量见工作计划，仍须独立完成浏览器和 NAS 验收。
- PDF 在服务端执行“设备 → 维修工具 → 设备附件 → 其他项目”的统一聚合排序。配置清单与询价单不显示价格；报价单保留单价、行小计和总计，隐藏分组小计。
- 报价客户信息包含姓名、电话、邮箱、地址，支持读取客户资料及手动修改；已发送版本保留独立快照。询价和报价 PDF 隐藏文档信息模块，业务时间和编号位于页眉右侧，页脚分别使用对应网址、地址、页码和生成时间规则。
- 后台分享记录支持分页、全字段模糊搜索和状态筛选，后台预览不增加客户查看次数，并支持关闭或重新启用未过期分享。
- 客户提交分享不会自动变成报价；“联系销售获取报价”会单独创建可追踪询价。管理员和业务员可协作查看询价，按负责人接手、联系并转换为各自独立的报价。
- 个人中心包含“我的分享记录”“我的询价”和“我的报价单”；报价发送、查看、撤回、归档和恢复均保留版本与审计记录。
- 管理后台编辑卡片已统一弹窗尺寸、边距、字号和输入框规格；设备编辑、配置分类、配置条目和添加设备分别采用适合内容量的卡片布局。
- 配置目录分类的“更多”菜单支持稳定的纵向展开和层级显示；配置条目编辑改为单列字段，编号、名称、描述和备注不会互相挤压。
- 设备编辑的配置模块采用搜索、筛选、展开/折叠分层布局；筛选位于左侧，展开/折叠位于右侧，标注和展开类操作按钮保持中文显示。
- 编辑卡片中的中文/英文切换只切换输入框示例及对应内容，字段标题保持中文；中英文数据分别提交并保存。
- 后台登录 Logo 已修复原始比例，目录编辑卡片增加必填项定位、语言按钮状态和关闭后的焦点恢复。
- 用户端选择设备型号后会将设备标题定位到固定导航栏下方；选择颜色会自动折叠设备概况并回到相同定位，系统启用“减少动态效果”时不使用平滑滚动。
- 已使用 `tb/pricelist.xlsx` 安全导入 67 个可匹配配置的人民币和美元参考价格；无法匹配的 8 项保持原值，导入前数据库备份和操作审计均已保留。
- 设备与配置目录的双语字段已从本地安全备份恢复并验证 API 输出；修复脚本可重复执行，默认先预览差异再写入。
- 设备目录的 Excel 导入、导出入口目前已从后台界面屏蔽，待完整的预览、校验、确认导入流程验收后再开放；相关 API 不作为日常操作入口。

数据库文件由 `BOTEN_DATABASE_PATH` 指定；未设置时使用 `backend/boten.db`。`js/data.js` 只保留初始化导入资料和用户端本地相册资源，不再作为产品文字的离线回退；API 不可用时页面会明确报错，避免显示过期或语言错误的数据。

用户端不显示或接收任何设备、配置、工具和附件价格；用户端及后台导出的配置清单、分享配置 PDF、询价 PDF 同样不含价格。价格数据继续保存在数据库和历史快照中，仅供管理员/业务员在报价编辑和报价 PDF 中使用。当前执行状态见 [2026-09-11 工作计划](docs/2026-09-11-task-plan.md)，旧检查点仅作历史记录。

## 角色权限

| 角色 | 主要权限 |
| --- | --- |
| 游客 | 浏览设备和进行临时选配 |
| 用户 | 注册/登录、保存配置、购物车、生成分享码 |
| 业务员 | 普通用户全部功能、设备与配置目录管理、查询分享记录、查看客户信息、管理自己的报价、下载报价 PDF |
| 管理员 | 业务员全部能力，以及设备、配置、账号和全部报价管理 |

## 技术栈

- 用户端与管理端：原生 HTML、CSS、JavaScript，无前端构建步骤
- API：FastAPI + Uvicorn
- 数据库：SQLite + Alembic 版本迁移
- 认证：Bearer 会话令牌，PBKDF2-SHA256 密码散列
- 字体：用户端、管理后台和 PDF 统一使用项目内嵌的 HarmonyOS Sans；浏览器和 NAS 不依赖系统字体。字体授权见 `assets/fonts/harmonyos-sans/LICENSE.txt`。
- PDF：ReportLab 后端生成，优先使用项目内嵌的 HarmonyOS Sans SC，缺失时回退系统中文字体或内置中文字体

## 本地运行

当前依赖与回归基线为 Python 3.12，Docker API 镜像同样使用 Python 3.12；不再使用旧 Python 3.8 环境安装本批依赖。所有命令都应在项目根目录执行。

首次安装：

```powershell
Set-Location E:\Project\CC-Project\benchshop
py -3.12 -m venv backend\.venv312
.\backend\.venv312\Scripts\python.exe -m pip install -r .\backend\requirements-dev.txt -c .\backend\constraints-py312.txt
.\backend\.venv312\Scripts\python.exe -m pip check
```

仅当创建全新的空数据库时，才执行初始化导入：

```powershell
.\backend\.venv312\Scripts\python.exe -m backend.seed
```

> `backend.seed` 会读取 `js/data.js` 并覆盖同编号的基础目录字段。已经通过后台维护过 `backend/boten.db` 后，不要把它当作日常启动命令反复执行。部署或迁移前应先备份数据库。

如数据库中还没有管理员账号：

```powershell
.\backend\.venv312\Scripts\python.exe -m backend.create_admin --email admin@example.com --name Administrator
```

日常启动需要两个 PowerShell 窗口。

窗口 1，启动 API（8001）：

```powershell
Set-Location E:\Project\CC-Project\benchshop
.\backend\.venv312\Scripts\python.exe -m uvicorn backend.main:app --host 127.0.0.1 --port 8001
```

窗口 2，启动带静态白名单和同源 API 代理的网站（8081）：

```powershell
Set-Location E:\Project\CC-Project\benchshop
.\backend\.venv312\Scripts\python.exe -m deploy.local_dev_server --mode isolated --port 8081
```

访问地址：

- 用户选配页：`http://127.0.0.1:8081/`
- 管理后台：`http://127.0.0.1:8081/admin/`
- API 文档：`http://127.0.0.1:8001/docs`
- 健康检查：`http://127.0.0.1:8001/api/v1/health`
- 数据库就绪：`http://127.0.0.1:8081/api/v1/ready`

不要在项目根目录使用通用 `http.server`：它可能公开数据库和源资料。本地代理监听所有网卡；局域网访问使用本机 IP 和 8081 端口，并按需设置防火墙。API 可保持仅监听回环地址。此开发服务不代替 NAS 的 Nginx 部署。

不要直接双击 `index.html` 以 `file://` 方式运行。用户端目录必须从 API 获取。

常用维护命令：

```powershell
# 运行自动化测试
.\backend\.venv312\Scripts\python.exe -m pytest -q

# 备份并保留最近 30 份
.\backend\.venv312\Scripts\python.exe -m backend.database_maintenance backup --keep 30

# 检查目录翻译、价格、图片和映射
.\backend\.venv312\Scripts\python.exe -m backend.audit_catalog

# 预览孤立上传图片（默认不删除）；核对后增加 --apply 执行清理
.\backend\.venv312\Scripts\python.exe -m backend.media_maintenance cleanup
.\backend\.venv312\Scripts\python.exe -m backend.media_maintenance cleanup --apply
```

## 项目结构

```text
.
├── index.html                 用户选配入口
├── admin/                    管理员/业务员后台
├── css/                      用户端样式、响应式和打印样式
├── js/
│   ├── runtime-config.js     API 地址配置
│   ├── catalog-api.js        后台目录加载与语言参数
│   ├── data.js               首次导入数据和本地相册资源
│   ├── language.js           用户端静态界面短文本翻译
│   ├── auth.js               注册、登录和会话
│   ├── cart.js               已保存配置、购物车和分享
│   └── ...                   状态、渲染、价格和 PDF 下载逻辑
├── backend/                  FastAPI、SQLite、仓储及维护脚本
├── tests/                    后端自动化测试
├── alembic.ini               数据库迁移配置
├── backups/                  本地数据库备份（不会提交到 Git）
├── uploads/                  后台上传的目录图片（不会提交到 Git）
├── tb/                       原始设备图片、配置图片、Excel/PDF 资料
├── codex.md                  历史开发与问题处理记录
└── README.md
```

## 数据维护约定

- 设备型号、标题、描述、启用状态和双币种价格在“设备目录”维护。
- 公共配置分类和配置项在“配置目录”维护，再在设备编辑卡片中选择该机型可用项。
- 共用图片和配置资料可以复用，但机型可选范围必须通过 `product_options` 单独映射。
- 机型专属提示写入设备与配置的中英文映射覆盖，不要直接修改共享配置说明。
- 配置与设备颜色图片可在编辑卡片直接上传 PNG/JPEG/WebP（最大 8 MB），也可填写已有相对路径。
- 删除配置时会先检查设备引用；配置分类必须为空才能删除，电机和供电基础分类不可删除。
- 用户端静态按钮/提示翻译位于 `js/language.js`；产品和配置业务文字由数据库提供。
- 批量价格资料优先保留在 `tb/` 并先执行编号匹配预览；当前可用的一次性资料为 `tb/pricelist.xlsx`，未匹配的编号不得自动新增或改写。
- 如目录中文显示异常，可先执行 `python -m backend.repair_catalog_translations` 查看差异；确认来源备份正确后再加 `--apply`。脚本只恢复双语文字字段，不修改价格、图片、映射和报价历史。

## 部署准备

阿里云 ECS 的安装、镜像离线导入、环境配置、空数据库处理及从 Albert-NAS 迁移完整业务数据库的操作，见 [阿里云 ECS 部署与数据库迁移指南](docs/2026-09-06-ecs-deployment-guide.md)。

### 2026-09-01 群晖部署状态

最新代码与数据库已在群晖 `BOTEN_NAS` 的 `/volume1/docker/benchshop` 启动验证：`api` 容器状态为 `healthy`，`web` 容器已运行并通过宿主机 `8080` 端口提供服务。局域网验收使用 `http://<NAS-IP>:8080/`，管理后台使用 `http://<NAS-IP>:8080/admin/`。

## 当前开发文档

- [项目执行规则（当前约定）](docs/project-execution-rules.md)
- [2026-09-11 工作计划与未完成事项](docs/2026-09-11-task-plan.md)
- [2026-09-11 界面与数据链路验收](docs/2026-09-11-ui-review.md)
- [2026-09-09 功能与界面修复计划及验收记录](docs/2026-09-09-task-plan.md)
- [2026-09-09 开发检查点](docs/2026-09-09-development-checkpoint.md)
- [2026-09-08 任务计划与验收清单](docs/2026-09-08-task-plan.md)
- [2026-09-07 任务计划与验收清单](docs/2026-09-07-task-plan.md)
- [网站设计要点](docs/website-design-system.md)
- [业务内容聚合与排序规范](docs/commerce-content-aggregation-standard.md)

本次首次启动曾因“表结构已存在但 Alembic 版本记录落后”而失败。已在核对现有表结构后以 `docker compose run --rm --no-deps --entrypoint python api -m alembic stamp 20260901_0005` 修正记录，再执行 `docker compose up -d --force-recreate` 成功启动。此操作只更新迁移版本，不会重建或删除业务数据。

当前代码适合本地验证。部署到外网前至少需要：

1. 选择 Linux 或 Windows 服务器并安装受支持的 Python。
2. 将 `backend/boten.db`、`uploads/` 和业务图片目录做独立、定时、可恢复的备份。
3. 使用 Nginx/Caddy 提供静态文件、HTTPS，并把同域 `/api/` 反向代理到 Uvicorn。
4. 用服务管理器运行 API（systemd、Supervisor 或 Windows Service），不要用开发终端长期托管。
5. 设置 `BOTEN_DATABASE_PATH` 和严格的 `BOTEN_CORS_ORIGINS`；生产环境不要允许 `null` 来源。
6. 将 `js/runtime-config.js` 的 API 地址改为生产地址；同域反向代理时可设为空字符串。
7. 每天运行 `python -m backend.cleanup` 关闭过期分享码，并监控磁盘、日志和 API 健康状态。

更详细的后端和接口说明见 [backend/README.md](backend/README.md)。

## 已知限制与建议路线

- 数据库仍是单机 SQLite；迁移文件已建立，多人高并发或多实例部署时仍建议迁移 PostgreSQL。
- 图片上传支持尺寸记录、双语替代文字、预览、排序和安全引用清理；裁剪、对象存储和重复图片检测仍未实现。
- 登录和分享限流保存在单个 API 进程内；生产环境应使用 Redis 或网关限流。
- 当前自动化测试集中在后端核心流程，并辅以浏览器人工回归；仍需引入可持续运行的浏览器端到端测试、结构化应用日志和 CI/CD。
- 后台 UI 已完成 1440px、1024px、720px 和 390px 自动化浏览器复验；触屏软键盘、不同系统缩放与真实设备仍保留人工验收。
- 目录结构与外键审计已无错误；已导入价格资料中可匹配的 67 项双币种参考价，剩余 8 项和部分配置图片仍待业务资料确认后填写。
- Excel 批量维护的前端入口尚未达到可用标准，现阶段保持隐藏，避免不完整流程影响正式目录数据。
- 可继续增加客户资料/公司抬头、报价有效期、税率折扣、报价状态流转、邮件发送和分享码撤回记录。

## 浏览器支持

推荐使用最新版 Chrome 或 Edge；Firefox 和 Safari 可完成常规选配，打印/PDF 效果需单独验证。

设备资料以 `tb/` 中的源文件和最终业务确认结果为准。

## 2026-09-09 本地开发检查点

- 配置、维修工具和设备附件编号统一使用保留连字符的规范形式，例如 `BT-WB`、`BTE-4240`、`BTC-0001`、`BTK-1001`；后台新增和编辑时自动规范化。
- 目录中英文名称不再重复包含自身编号；用户端选配、购物车、分享、询价、报价、客户中心以及统一 PDF 均分行显示编号和名称。
- 本机数据库完成 111 条目录记录审计并规范化 68 条，无编号冲突、非规范编号或重复名称前缀；历史业务快照不改写，在读取和 PDF 导出时兼容格式化。
- PDF 显式非正数量过滤、报价金额重算、设备图片安全清理、弹窗焦点与长报价列表性能优化已完成。
- 当前完整自动化测试为 108 项通过；本检查点仅提交代码与文档，不包含源 Excel、截图、样稿 PDF、临时数据库、`tmp/` 或数据库备份，也不执行部署。

## 2026-09-05 本地发布检查点

- 用户端产品入口已调整为顶部导航抽屉，支持检测设备、维修工具和设备附件；首次访问默认进入 CR1016，刷新会恢复当前设备或目录视图以及设备内配置分类。
- 维修工具和设备附件可独立加入购物车，并在购物车中分别聚合；购物车固定按“设备、工具、附件”排序，设备按加入顺序排列。
- 工具和附件目录在移动端保持双列，已选内容通过底部固定入口查看；卡片使用边框、强调色和勾选标记反馈已选状态。
- 用户配置 PDF 继续保持无价格边界。批量处理限制调整为最多 20 台设备、最多 100 个购物车项目，工具和附件的购买数量不会展开为重复项目计数。
- 个人中心已提供资料、账号、我的分享记录和我的报价单；分享、询价和报价保持独立业务对象，客户只有主动提交询价后才进入后台待处理列表。
- 后台分享详情按设备、工具、附件聚合，分享列表按实际数量显示摘要；表格菜单、筛选栏、API 状态和后台返回主页入口完成统一整理。
- 当前数据库迁移头为 `20260905_0018`。本轮完整自动化测试共 93 项通过，数据库维护检查通过。本检查点仅提交 Git；Albert-NAS 与 BOTEN-NAS 均不在本轮同步范围内。
# 2026-09-11 本机增量状态

本轮新增需求尚未全部完成。已实现个人中心分享详情行布局和分享码标题、分享/询价按钮简化、购物车选配分类只读预览，以及工具/附件分类创建编辑与分组布局。购物车预览和分类筛选通过隔离浏览器回归，前端契约 58 项通过；个人中心新增布局仍待浏览器专项验收。

设备语言可见性与排序、业务进度合并、购物车先分享再导出、PDF 文件名及页内编号统一仍待实施。没有提交 Git、部署 NAS 或修改真实数据库。详细状态见 [任务计划](docs/2026-09-11-task-plan.md) 最新执行检查点；不可将此前全量测试结果视为本轮完整验收。
