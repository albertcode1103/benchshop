# 项目协作入口

- 开始项目工作前，先阅读 `docs/CHANGELOG.md` 的维护约定、当前状态和有效待办，以及 `docs/project-execution-rules.md`。
- `docs/CHANGELOG.md` 是工作状态、每日进展与有效待办的唯一持续更新入口；规则和技术规范仍以各自文件为准。
- 在功能完成、验证完成、决策或阻断变化、Git 操作及发布验收等必要节点，按实际日期更新日志。同一天补充同一日期节，同步核对当前状态和有效待办，不另建零散进度或工作计划文件。
- 多个对话共同维护：修改前重读相关段落，只合并本任务有证据的增量，保留其他对话的有效记录，不覆盖整份日志。区分已实现、本机验证、Git 状态与远端部署，不将局部测试写成全量验收。
- 规则变化先更新对应规则文件，再在日志中引用；维护日志不构成 Git 提交、推送、部署或正式数据操作的授权。
